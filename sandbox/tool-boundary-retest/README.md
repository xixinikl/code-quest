# Agent 工具边界迁移复测

这是第 10 章完成至少 24 小时后才开放的换业务案件。原任务是让 Agent 查询订单，本案仍然要查询订单，但请求额外携带了内部文件路径。

目标不是背住“禁止 `/internal`”这一条，而是检查四层边界是否都成立：工具注册表说明能力，Schema 限制参数形状，权限门确认资源归属，审计与失败回退让拒绝可追踪且不泄露内部信息。

运行 `npm test` 会得到预期的 **2 passed / 1 failed**。失败故意保留在服务端：当前实现只拦截一个明显的路径前缀，没有通用的资源归属检查，不能接受为生产修复。
