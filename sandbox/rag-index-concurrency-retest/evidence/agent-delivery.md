# Agent 交付说明

当前实现采用“先 SELECT，再 UPDATE”的领取方式，没有把状态条件放进同一个原子写操作。两个 Worker 能同时读到 `pending`。

建议修复范围仅限索引任务仓库、数据库约束和对应测试。领取动作应原子地把一条 `pending` 任务改为 `running`，未抢到的 Worker 返回 `null`；chunks 需要业务唯一约束兜底；完成状态与 chunks 写入要有明确事务边界。

不要修改上传按钮来掩盖后端竞争，也不要读取任何真实项目或外部知识库。
