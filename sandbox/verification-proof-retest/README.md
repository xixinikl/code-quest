# 验收证据迁移复测

这是第 11 章完成至少 24 小时后开放的新案件。原任务是修复保存后刷新丢失，本案换成“Agent 交付了全绿报告，但报告绑定的是旧源码”。

本关训练的不是背 `sourceHash`，而是把失败复现、单测、集成测试、手动路径、源码版本和回归风险串成可信验收。Network 正常只能说明一次请求成功，不能独立证明旧问题已经修好。

运行 `npm test` 会得到预期的 **2 passed / 1 failed**。失败故意保留在报告校验器：它把全绿报告直接判为可信，漏掉了源码版本绑定。
