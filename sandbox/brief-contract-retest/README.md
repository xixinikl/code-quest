# Agent 委托契约迁移复测

这是第 12 章完成至少 24 小时后开放的换业务案件：给客服知识库增加批量导入。模糊委托只说“接入上传”，Agent 因此扩大了修改范围，还没有可观察验收和回滚。

本关训练背景、目标、约束、验收和风险不是格式装饰，而是让 Agent 能安全执行的工作契约。运行 `npm test` 会得到预期的 **2 passed / 1 failed**；失败故意保留在契约校验器，因为它没有要求回滚方案。
