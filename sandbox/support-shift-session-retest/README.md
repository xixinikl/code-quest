# 夜班客服登录态复测沙盒

这是第 3 章的延迟变式复测，不读取真实项目，也不会由学习应用执行。

## 新案件

客服在夜班工作台登录成功，浏览器也拿到了刷新凭证。认证服务滚动重启后，页面下一次续期却返回 `401 SESSION_EXPIRED`，全班客服被迫重新登录。

原始实现把刷新会话放在进程内 `Map`。测试会模拟服务重启，用同一个 SQLite 数据库创建新的仓库实例，检查刷新凭证能否继续被验证。

## 手动运行

```bash
npm test
```

应用只读取生成的 `test-results.json`，不会替你运行命令。
