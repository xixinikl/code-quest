# 组件剧场：按钮为什么一点击就乱跳

这是一个独立的前端阅读沙盒。请先看 `frontend/ProfilePanel.jsx`，再根据交互记录和测试报告修复它。

## 运行

```bash
npm test
```

应用不会替你执行命令。修复时只修改 `frontend/`，不要修改 `tests/` 或 `evidence/`。

## 事故背景

用户点击“加载资料”后，按钮、提示和资料卡片由不同的状态来源控制，页面会先显示成功，失败时却没有可读反馈。目标是让一个状态所有者描述加载、成功和失败，并让组件渲染与状态保持一致。
