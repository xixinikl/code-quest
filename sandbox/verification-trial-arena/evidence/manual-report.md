# 手动复测报告

## 操作步骤

1. 打开画布页面。
2. 输入标题“验收试炼画布”并点击保存。
3. 确认 Network 里 `POST /api/canvases` 返回 201。
4. 刷新页面。
5. 确认列表里仍然有“验收试炼画布”。

## 期望结果

刷新后，刚保存的画布仍然存在。

## 实际结果

刷新后，画布仍然存在。

## 证据边界

这份手动报告能证明浏览器主路径可用，但还需要和当前源码 hash、单元测试、集成测试一起看，才能说交付可信。

## 回归风险

- 只测了单用户保存，没有覆盖多人同时保存。
- 只测了正常标题，没有覆盖空标题和超长标题。
