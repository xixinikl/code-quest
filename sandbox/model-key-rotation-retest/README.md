# 模型密钥熔炉：密钥轮换复测

供应商轮换 API Key 后，AI 助手开始返回 401。有人想把新 Key 写进前端请求里，导致浏览器 Network 可以直接看到秘密。

本案要沿三条边界判断：浏览器只能交用户问题，自有 API 从服务端环境变量读取密钥，模型供应商的认证失败要被转换成不泄露内部配置的可行动错误。

运行 `npm test`。初始结果为 2 passed, 1 failed；失败用例证明网关仍然把旧 Key 写死在服务端代码里，无法随环境轮换。
