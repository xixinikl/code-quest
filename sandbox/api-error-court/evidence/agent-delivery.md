# Agent 交付说明

已完成 Project Brief 创建接口：

- `POST /api/briefs` 能接收请求体。
- 正常请求会返回 201。
- 异常时前端会提示“提交失败”。

缺少的验收证据：

- 没有测试缺少 `userGoal` 的请求体。
- 没有区分 400 参数错误和 500 服务器异常。
- 响应体没有字段级错误，用户不知道该补什么。
- 后端日志没有把 requestId、状态码和校验原因整理成可追踪证据。
