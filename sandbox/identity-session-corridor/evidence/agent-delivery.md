# Agent 交付说明

已完成登录功能：

- 登录接口返回 200。
- 页面能显示“见习开发者”。
- 提供 `/api/me` 用于读取当前用户。

缺少的验收证据：

- 没有证明登录后写入 Cookie 或其他可刷新恢复的凭证。
- 没有证明服务端登记了 session。
- 没有刷新页面后再次访问 `/api/me` 的 Network 证据。
- 没有解释 401 是前端没带凭证，还是后端没有会话记录。
