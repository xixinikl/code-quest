# Agent 交付说明（待审查）

我已经接入 AI 聊天功能，页面可以调用模型并展示结果。

## 已完成

- 前端新增输入框和“点燃模型熔炉”按钮。
- 调用 AI 服务后显示完整回复。
- AI 失败时显示“AI 调用失败”。

## 测试

- 手动输入 prompt，看到返回文本。

## 需要你审查

这份交付没有证明四件事：

- 前端包里是否没有真实或演示 API Key。
- 浏览器 Network 里是否没有第三方 AI Authorization。
- 后端是否只从 `process.env.AI_API_KEY` 读取密钥。
- 失败时是否返回结构化错误，并且不把密钥放进响应或日志。
