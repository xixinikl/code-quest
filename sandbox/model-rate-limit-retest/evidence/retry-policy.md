# 模型依赖限流处理约定

当模型供应商返回容量型 `429` 时，自有 API 不能把它伪装成未知 `500`。

- 对调用方返回 `503 Service Unavailable`。
- 错误码使用 `MODEL_DEPENDENCY_BUSY`，说明故障位于模型依赖边界。
- 同时返回 `requestId` 与 `retryAfterSeconds`，让页面可以解释等待时间。
- 日志保留上游原始 `429`，用于值班人员关联证据。
- 不自动无限重试；批处理只允许由队列或用户在约定时间后重新发起。
