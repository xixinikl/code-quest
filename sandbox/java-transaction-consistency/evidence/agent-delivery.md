# Agent 交付说明

已完成草稿保存功能：

- `POST /api/drafts` 能创建草稿。
- 创建成功时返回 201。
- 保存后会写一条审计日志。

缺少的验收证据：

- 没有模拟用户连点两次保存按钮。
- 没有证明同一个 `Idempotency-Key` 只会写一条记录。
- 没有证明同一用户的 `clientMutationId` 有唯一约束。
- 没有证明审计日志失败时数据库会回滚，避免留下半截草稿。
