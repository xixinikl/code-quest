# Agent 交付说明（待审查）

我已经让 Agent 可以调用工具，能搜索文档，也能更新项目状态。

## 已完成

- 新增 `searchProjectDocs` 工具。
- 新增 `updateProjectStatus` 工具。
- 前端能点击按钮触发工具调用。

## 测试

- 手动点了按钮，看到工具返回结果。

## 需要你审查

这份交付没有证明五件事：

- 工具描述是否写清只读/写入边界。
- 参数 schema 是否真的拦截缺字段和非法枚举。
- 当前用户没有权限时是否拒绝写入。
- 工具内部失败时是否结构化回退。
- 合法工具调用是否留下审计日志。
