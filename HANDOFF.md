# 码上冒险：后续 Agent 交接入口

## 当前状态

`cx/ai-career-rpg-home` 分支，基于冻结 `main` 开发；当前阶段成果已经推送到 GitHub。

### 当前事实快照（2026-07-17）

- 当前远端：`https://github.com/xixinikl/code-quest.git`
- 当前工作分支：`cx/ai-career-rpg-home`
- 当前已推送功能基线：本文件随最新提交推送后，以 `git ls-remote origin refs/heads/cx/ai-career-rpg-home` 输出为准；当前本轮之前远端已到 `ea7b076 fix(rpg): humanize lab artifact rewards`，本次提交会晚于它。
- 当前已推送交接基线：本文件提交后应晚于 `772f66e`，包含另一台电脑拉取、核对、继续开发和不能直接合并的说明。最终远端 hash 仍以 `git ls-remote origin refs/heads/cx/ai-career-rpg-home` 为准。
- 当前本地核对：推送完成后，`git status --short --branch` 应显示 `cx/ai-career-rpg-home...origin/cx/ai-career-rpg-home` 且没有未提交文件，说明本地与远端一致。
- 当前远端核对：另一台电脑拉取后 `git log --oneline -5` 应看到本次交接文档提交和 `772f66e docs(rpg): record frontend performance lab verification`，或更晚提交；如果只看到 `65e3603`、`5651c18`、`0fb18e7`、`0f7c433`、`c587a23`、`547ed6f`、`4370ddf`、`1b3cf5d`、`9171ac8`、`1a40fe4`、`e10069f`、`fa12dbc`、`2dd44d3`、`ca2c7aa`、`3fe3db1` 或默认分支提交，说明还没拉到最新交接/体验细修。
- 远端默认 HEAD：`git ls-remote --symref origin HEAD` 指向 `feat/guided-learning-bridge`，不是本分支；另一台电脑必须显式切到 `cx/ai-career-rpg-home`，不要只用 clone 后默认分支继续。
- 给协作者的最短说明：不要直接用默认分支；拉仓库后必须切到 `cx/ai-career-rpg-home`。当前至少应看到 `ea7b076 fix(rpg): humanize lab artifact rewards` 和本次交接文档提交，或更晚提交；进入项目后必须 `nvm use` 到 Node `24.13.1`。
- 他具体怎么拉：如果另一台电脑没有本项目，按“首次 clone”执行；如果已经 clone 过，按“已有仓库更新”执行；如果他本地有未提交改动，先新建自己的分支或 stash，不要直接覆盖。

### 另一台电脑首次 clone 照抄

```bash
git clone https://github.com/xixinikl/code-quest.git
cd code-quest
git fetch origin
git switch -c cx/ai-career-rpg-home --track origin/cx/ai-career-rpg-home
git log --oneline -5
nvm install
nvm use
npm install
npm run verify
npm run dev
```

拉完后 `git log --oneline -1` 应显示：

```bash
本次交接文档提交，或晚于 772f66e 的提交
```

如果显示的是 `feat/guided-learning-bridge`、`main` 上的提交，或早于 `772f66e`，说明没有拉到今天上传的内容。

### 已有仓库更新照抄

```bash
cd code-quest
git fetch origin
git switch cx/ai-career-rpg-home
git pull --ff-only origin cx/ai-career-rpg-home
git log --oneline -5
nvm use
npm install
npm run verify
npm run dev
```

如果 `git switch cx/ai-career-rpg-home` 提示本地没有这个分支：

```bash
cd code-quest
git fetch origin
git switch -c cx/ai-career-rpg-home --track origin/cx/ai-career-rpg-home
git log --oneline -5
```

### 如果他本地有改动，先保护改动再拉

```bash
git status --short
git switch -c cx/my-local-work
git add .
git commit -m "wip: save local work"
git fetch origin
git switch cx/ai-career-rpg-home
git pull --ff-only origin cx/ai-career-rpg-home
```

如果他不确定本地改动有没有用，不要 `checkout -B` 强制覆盖；先把改动提交在 `cx/my-local-work`，后面再比较或 cherry-pick。

### 拉完必须确认

```bash
git status --short --branch
git rev-parse HEAD
git rev-parse origin/cx/ai-career-rpg-home
git ls-remote origin refs/heads/cx/ai-career-rpg-home
node -v
npm run verify
```

本地 `HEAD`、`origin/cx/ai-career-rpg-home` 和 `git ls-remote` 的 hash 应一致；`node -v` 应是 `.nvmrc` 指定的 `v24.13.1`，`npm run verify` 应通过。如果不一致，不要继续开发，先重新 `git fetch origin` 并切回 `origin/cx/ai-career-rpg-home`。

### 拉完后怎么继续开发

1. 新电脑确认完整后，继续在 `cx/ai-career-rpg-home` 上开发，或从它再切个人分支，例如 `git switch -c cx/frontend-performance-polish`。
2. 不要直接合并 `main`。当前分支是阶段成果，不是 ready 合并态；合并前还缺 PR 审查、桌面和 390px 关键路径视觉终审、真人试玩、以及 Java/前端后续章节更深抽检。
3. 如果另一台电脑只是要接着我现在的工作，优先做前端第 3 关“首屏观测塔”剧情到 Lab 的完整浏览器验收，清理残留的第一章保存链路词，再补测试和记录。
4. 如果要开 PR，先开 Draft PR；PR 描述必须写清“自动化通过不等于真人学会”，不要把当前状态包装成最终产品。

- AI 应用开发路线：15 章；Java 后端路线：5 章；前端工程路线：5 章。三条路线均已进入岗位档案，Java/前端不再是空白占位。
- 已实现：暗色神秘 RPG 舞台、章节专属背景、剧情角色立绘、可爱宠物/伙伴、地点航线、流程交接、名词解释、关键代码逐行导读、证据任务、Agent 委托、验收和面试复盘。
- 最新体验收口：第 2 章产品链路不再串到 AI API 章；教学桥关键控件已暗色 RPG 化；代码导读新增“为什么看这一行 / 检查点 / 下一份证据”，第 1 章 `response.ok` 明确提示不能证明数据库写入。
- 最新实战收口：第 1 章测试报告页新增「验收证据桥」；第 1 章保存失败报告会优先指向“数据层写库没接上 / repository `INSERT` 缺证据”，不会误串到第 2 章会话保存话术。第 1 章实战后半段已补齐沙盒验收、Agent 委托、交付审查、因果解释和面试迁移的任务卷轴与流程棒，明确页面绿灯、接口 201、数据库写入和刷新读回是四种不同证据。第 2 章 CanvasStorm 失败报告会把方向筛选、会话保存、空目标输入解释为三个断点，并用「红灯总指挥」给出排查顺序和 Agent 口令。第 3 章登录态实战已补齐身份路线、凭证存储、401 反证、刷新复查、Agent 委托、交付审查和面试复盘的剧情向导、任务卷轴与流程接力。第 4 章接口审判庭实战已补齐请求体证词、状态码判词、错误体修复、日志串证、Agent 委托、交付审查和面试复盘的导演层，并用 `flowItemIndex` 避免后半段流程高亮错位。第 5 章一致性熔炉实战已补齐 Network 双轨、幂等锤印、唯一约束城门、事务炉心、Agent 委托、交付审查和面试复盘的导演层。
- 最新自动化验证：Node `24.13.1` 下完整 `npm run verify` 通过，包含格式、Lint、TypeScript、11 个测试文件 / 187 个测试、生产构建和 TeachingBridge 懒加载检查。Vite 主包体积 warning 是已知债务，不是失败。环境反例：如果终端仍在 Node `18.20.8`，会因 `node:sqlite` 缺失和 jsdom ESM 依赖失败；先执行 `nvm use` 再验收。
- 最新浏览器验收：隔离 API `4369`、临时 SQLite `/tmp/code-quest-r303.sqlite`、Vite `5219`；第 1 章从剧情探索完整收集 8/8 线索 → 伙伴会合 → 实战 Lab 通过。桌面 1280 和 390px 手机均无横向溢出，控制台 error 为 0；后半段 Agent、审查、因果和面试迁移步骤都显示新任务卷轴与流程接力。合并前追加抽检过首页和路线大厅：隔离 API `4370`、临时 SQLite `/tmp/code-quest-r304.sqlite`、Vite `5220`；桌面 1280 与 390px 下序章、证据选择、领取委托、三路线大厅均无横向溢出，控制台 error 为 0。
- 最新岗位路线收口：Java/前端第 2-5 章深链进入剧情探索后会持续显示「当前路线身份」铭牌，标明 Java 后端或前端工程成长路线、当前案件和当前地点，避免用户从封面进入剧情后失去岗位方向。隔离 API `4371`、临时 SQLite `/tmp/code-quest-r305.sqlite`、Vite `5221` 下，8 个深链桌面 1280 与 390px 均无横向溢出，暗色背景保持，控制台 error 为 0。
- 最新实战深链收口：Java 第 2 关从 `#chapter-java-2` 完成教学后，伙伴会合页显示 `Java 后端 / 岗位路线 · 第 2 章`，进入 Lab 后保持 `Java 后端 · 第 2 关`。事务 Lab 可见文案已从旧“保存草稿”语境修正为订单、库存、下单、事务回滚语境；桌面 1200 与 390px 无横向溢出，控制台无 error。剩余 Java/前端第 3-5 关仍需逐关做同类 Lab 抽检。
- 最新岗位 Lab 语境收口：Java 第 3 关缓存 Lab 的可见流程已改为旧数据、缓存命中、数据库版本、TTL、异步刷新和复测收敛；前端第 4 关无障碍 Lab 的可见流程已改为语义、键盘、读屏、焦点、390px 和回归证据，不再显示生产变量、备份恢复或上线门禁。隔离 API `4375`、临时 SQLite `/tmp/code-quest-r308.sqlite`、Vite `5225` 下，`#chapter-frontend-4` 深链到 Lab 桌面 1200 与 390px 无横向溢出，控制台无 error。
- 最新事故 Lab 语境收口：Java 第 5 关事故 Lab 的可见流程已改为报警、requestId、异常栈、版本号、影响范围、止血/回滚和恢复复测，不再显示上线门禁、生产变量或备份恢复模板词。隔离 API `4376`、临时 SQLite `/tmp/code-quest-r309.sqlite`、Vite `5226` 下，`#chapter-java-5` 深链到 Lab 桌面 1200 与 390px 无横向溢出，控制台无 error。
- 最新前端 Lab 语境收口：前端第 3 关性能 Lab 的可见流程已改为首屏瀑布图、接口 TTFB、Server-Timing、X-Cache、React 渲染画像和第二次访问复测；前端第 5 关测试 Lab 的可见流程已改为旧故障红灯、报告校验器、浏览器手动复测、sourceHash、回归风险和 Agent 交付审查，不再露出 `/api/canvases` 或 `验收试炼画布` 这类 AI 主线保存链路词。浏览器深链验收待补录。
- 最新前端第 3 关性能语境收口：`#chapter-frontend-3` 第一幕线索不再写“保存后刷新慢”，改为首屏白屏、筛选慢、列表出现慢和滚动卡顿；剧情证据补入 `Server-Timing` 与 `X-Cache: MISS/HIT`；Lab 新手先读卡和证据示范卡不再掉回第一章 `response.ok / 数据库写入 / 保存链路` 默认模板。隔离 API `4406`、临时 SQLite `/tmp/code-quest-r330.sqlite`、Vite `5256` 下，完整收集 10/10 剧情线索 → 伙伴会合 → 进入实战 Lab；Lab 首屏显示 TTFB、Server-Timing、X-Cache、backend.log 和 render profile，桌面 1200 与 390px 均无横向溢出，暗色背景，控制台 error 为 0，旧保存/数据库词检查全为 false。
- 最新前端第 5 关 Lab 接力修复：从 `#chapter-frontend-5` 走完 8/8 剧情线索、伙伴会合并进入前端测试 Lab，发现首题 `流程接力小剧场` 写成「旧故障 把线索交给 旧故障」。已修成第一棒从 `剧情现场 / 事故线索` 交给当前棒，最后一棒再交给 `结案卷宗 / 最终验收`，避免新手困惑“谁把什么交给谁”。隔离 API `4408`、临时 SQLite `/tmp/code-quest-r332.sqlite`、Vite `5258` 下，前端第 5 关深链桌面 1280 与 390px 均无横向溢出，暗色背景，控制台 error 为 0，旧保存链路词检查全为 false。`npm run verify:quick` 通过 11 个测试文件 / 189 个测试。
- 最新前端第 5 关 Lab 证物名修复：Lab 导师、任务卷轴、冒险日志、能力印记、流程翻译、接力小剧场、保存回执、结案卷宗和 Agent 委托都不再把 `failing-before`、`passing-after-stale`、`network-test-run` 这类内部 ID 当作可见学习材料，而是读取 `artifactGuides[id].place` 显示「第 1 棒证据：旧问题红灯」「验收门禁：报告校验器」等人话证物名。隔离 API `4409`、临时 SQLite `/tmp/code-quest-r333.sqlite`、Vite `5259` 下确认前端第 5 关封面进入正常，第一幕主动复述门槛有效；本轮追加渲染测试覆盖可见正文不得出现 `failing-before` / `network-test-run`。后续仍需继续做完整作答提交到结案页的浏览器录入式验收。
- 最新前端第 5 关沙盒证物收口：`sandbox/frontend-testing-proof` 的 README、Network、手动报告、失败复现、过期报告、后端日志和 Agent 交付说明已全部改为任务列表筛选回归语境，固定 `GET /api/tasks?status=blocked`、DOM 可见列表、`POST /api/reports/verification` 和 `sourceHash` 证据链；新增 `src/sandboxEvidence.test.ts` 防止 `/api/canvases`、保存画布、验收试炼画布等旧词回流。Node `24.13.1` 下 `npm run verify:quick` 通过 11 个测试文件 / 187 个测试；沙盒自身 `npm test` 仍按练习设计失败 5 项，用于训练用户修报告校验器。
- 最新前端第 5 关浏览器文案收口：隔离 API `4401`、临时 SQLite `/tmp/code-quest-r325.sqlite`、Vite `5251` 下，从 `#chapter-frontend-5` 真实浏览器发现主动复述、边界用例、Agent 示例和 Lab 证据表达卡仍有第一章保存/数据库例子；已改为筛选状态、DOM 可见行、Network、`sourceHash` 和回归风险。桌面 1280 与 390px 手机 DOM 旧词检查为空，暗色背景，控制台 error 为 0。
- 最新 Java 第 4 关上线港归属收口：从 `#chapter-java-4` 真实浏览器发现剧情线索和 Lab 示范卡仍露出 AI 第 14 章 / 第一章保存链路旧词。已把 Java 上线港五幕剧情、沙盒证物、Lab 先读卡和证据表达示范改为订单服务上线语境：`order-service`、`POST /api/orders`、`JWT_SECRET`、`PAYMENT_API_URL`、订单表迁移、`/actuator/health`、下单成功率、支付回调失败率和回滚后验证。隔离 API `4403`、临时 SQLite `/tmp/code-quest-r327.sqlite`、Vite `5253` 下，完整收集 10/10 剧情线索并进入实战 Lab，控制台 error 为 0。
- 最新教学桥地图收口：Java 第 2-5 关、前端第 2-5 关已补齐独立 `chapterCinematics` 地图镜头契约，不再因为缺少配置回退到第一章 `数据接力路线 / 断流档案河 / 失忆数据库`。隔离 API `4379`、临时 SQLite `/tmp/code-quest-r312.sqlite`、Vite `5229` 下，`#chapter-java-4` 与 `#chapter-frontend-5` 点击开始闯关后保持岗位路线语境，桌面 1200 无横向溢出，控制台 0 error。
- 最新实战导演台收口：实战页把「实战剧情向导」「本步任务卷轴」「流程接力小剧场」合并到同一个 `任务导演台` 视觉容器中，桌面并排显示角色地点、学习目的、证据入口和上一棒/当前棒/下一棒，手机改为单列，降低用户在多张卡之间来回滚动拼流程的成本。完整 `npm run verify` 通过：10 个测试文件 / 180 个测试、生产构建和 TeachingBridge 懒加载检查均通过；隔离 API `4380`、临时 SQLite `/tmp/code-quest-r313.sqlite`、Vite `5230` 下，390×844 剧情路径无横向溢出，暗色背景，控制台 0 error。
- 最新辅助卷宗收口：实战页的完整流程图、案件路线牌和实战接力板已移入默认折叠的 `辅助卷宗`，主屏只保留完整流程标题、当前这一棒和展开按钮；点击「展开流程地图」后仍能看到完整辅助资料。完整 `npm run verify` 通过：10 个测试文件 / 180 个测试、生产构建和 TeachingBridge 懒加载检查均通过；隔离 API `4381`、临时 SQLite `/tmp/code-quest-r314.sqlite`、Vite `5231` 下，第一章剧情入口 390×844 无横向溢出，暗色背景，控制台 0 error。
- 最新岗位 Lab 护栏：新增渲染级回归测试，覆盖 Java 上线港、前端性能塔和前端回归试炼场。岗位 Lab 默认只显示 `任务导演台`、`辅助卷宗` 和 `当前这一棒`，不会直接铺满案件路线牌/接力板；点击「展开流程地图」后才显示完整流程、案件路线牌和实战接力板，并且不出现 AI 主线旧词、`/api/canvases` 或 `验收试炼画布`。`App.test.tsx` 单文件 55 项通过，`npm run verify:quick` 通过 10 个测试文件 / 181 个测试。
- 最新浏览器发现与修复：隔离 API `4390`、临时 SQLite `/tmp/code-quest-r317.sqlite`、Vite `5240` 下，Playwright 从 `#chapter-frontend-5` 进入前端第 5 关，逐幕收集 8/8 线索并进入实战会合与 Lab；剧情页角色和地点按 `测试仲裁官 → 交互取证师 → 路径审查官 → 交付守门人` 变化，控制台 error 为 0。浏览器发现 Lab 第一题「复现旧故障」却显示当前棒为「单测」的流程错位；已给 `frontend-testing-proof` 补岗位专属 `flowItemIndex`，现在第一题停在「旧故障」，后续单测、集成、浏览器、接收各自对应正确流程棒。完整 `npm run verify` 通过：10 个测试文件 / 181 个测试、生产构建和 TeachingBridge 懒加载检查均通过。
- 最新导演台流程翻译：实战 `任务导演台` 新增 `本幕流程翻译`，把上一棒交来的材料、当前只盯的证据和下一棒接收对象翻译成一段人话，并固定“先看当前地点和关键材料 → 再说能证明/不能证明 → 最后决定下一份证据”的三步顺序。该卡跟随 `LabConfig.flowItems` 动态生成。验证：完整 `npm run verify` 通过 10 个测试文件 / 181 个测试；隔离 API `4391`、临时 SQLite `/tmp/code-quest-r318.sqlite`、Vite `5241` 下，Playwright 从 `#chapter-frontend-5` 收集 8/8 线索进入前端测试 Lab，首屏可见 `本幕流程翻译`、`能证明什么、不能证明什么` 和下一棒 `守报告校验器`；390×844 下 `scrollWidth = clientWidth = 390`，暗色背景，控制台 error 为 0。
- 本轮实战保存接力回执：保存一道实战回答后，`LabStepReceipt` 会显示接力信息，把“刚刚停在 → 现在进入 → 只盯住哪份证据”摆出来，避免用户被自动跳步弄丢流程。隔离 API `4393`、临时 SQLite `/tmp/code-quest-r320.sqlite`、Vite `5243` 下，从 `#chapter-frontend-3` 收集 10/10 线索进入前端性能 Lab，保存第一题后可见「刚刚停在 Network」「现在进入 接口」「只盯住『拆后端等待』」，桌面 1200 与 390px 均无横向溢出，暗色背景，控制台 error 为 0。
- 本轮前端第 5 关剧情修复：浏览器发现 `#chapter-frontend-5` 剧情线索仍从通用 AI 验收章露出 `/api/canvases` 和旧保存路径。已给前端测试路线新增专属剧情/journey 重写，线索改为筛选交互、可见列表、DOM、Network 和报告指纹，并新增运行时回归测试防止 `TeachingBridge` 再串回 AI 保存链路。隔离 API `4394`、临时 SQLite `/tmp/code-quest-r321.sqlite`、Vite `5244` 下，桌面 1200 与 390px 均无横向溢出，暗色背景 `rgb(7, 12, 20)`，控制台 error 为 0；页面正文不再出现 `/api/canvases`、`canvas-save-persistence`、保存画布、验收试炼画布或旧保存链路。
- 本轮前端第 4 关教学层修复：`frontend-accessibility-proof` 不再直接继承第 14 章上线门禁 teaching map/code tour；项目地图改为用户任务、语义与状态反馈、移动端复测、可访问性哨塔、回归守门和无障碍交付决定，代码导读改为 `docs/accessibility-checklist.md` 与 `frontend/accessibility-audit.md`。完整 `npm run verify` 通过 10 个测试文件 / 185 个测试；隔离 API `4395`、临时 SQLite `/tmp/code-quest-r322.sqlite`、Vite `5245` 下，`#chapter-frontend-4` 桌面 1280 与 390px 均无横向溢出，暗色背景，控制台 error 为 0，正文不再出现上线门禁、上线计划、生产变量、生产环境、备份恢复或 `AI_API_KEY`。
- 本轮 Java 第 5 关教学层修复：`java-production-incident` 不再直接继承第 14 章上线门禁 teaching map/code tour；项目地图改为事故窗口、日志与运行环境、影响范围、报警哨塔、止血决策门和事故结论，代码导读改为 `docs/incident-response-timeline.md` 与 `server/IncidentTimeline.java`。完整 `npm run verify` 通过 10 个测试文件 / 186 个测试；隔离 API `4396`、临时 SQLite `/tmp/code-quest-r323.sqlite`、Vite `5246` 下，`#chapter-java-5` 桌面 1280 与 390px 均无横向溢出，暗色背景，控制台 error 为 0，正文不再出现上线门禁、上线计划、生产变量、生产环境、备份恢复或 `AI_API_KEY`。
- 尚未声称完成：真人学习效果、所有章节达到第一章同等细致程度、真实沙盒修复与报告回读、全站最终视觉终审、分支合并审查，以及第 1 章实战后半段“沙盒验收 → Agent 委托 → 交付审查 → 因果解释 → 面试迁移”的对白进一步打磨。
- 版本状态：当前分支可以被另一台电脑完整拉取继续开发，但尚未合并到 `main`。不建议直接合并；如果时间紧，先开 Draft PR 或继续在本分支开发。转 ready/合并前仍要做 PR 审查、关键路径桌面与 390px 视觉终审，并明确“自动化通过不等于真人学会”。

### 2026-07-17 最新跨电脑核对

- 当前功能基线至少包含 `772f66e docs(rpg): record frontend performance lab verification`；远端 HEAD 请以 `git ls-remote origin refs/heads/cx/ai-career-rpg-home` 为准。
- 当前沙盒内 `xixi-dev-system profile sync`、`doctor --project .` 和 `updates --project .` 已通过。
- 另一台电脑拉下来后，用 `git rev-parse HEAD`、`git rev-parse origin/cx/ai-career-rpg-home`、`git ls-remote origin refs/heads/cx/ai-career-rpg-home` 三个值互相对照；三者应一致或本地 HEAD 是刚拉下来的同一提交。
- 当前仍是“可继续开发的阶段分支”，不是 ready 合并态。最新完整 `npm run verify` 已在 Node `24.13.1` 下通过；合并前仍要做桌面与 390px 关键路径视觉终审、PR 审查和真人试玩。

### 2026-07-16 跨电脑/合并决策补充

- 本地分支 `cx/ai-career-rpg-home` 已与 `origin/cx/ai-career-rpg-home` 对齐，`git status --short --branch` 没有未提交文件。
- 远端核对命令：`git ls-remote origin refs/heads/cx/ai-career-rpg-home` 的输出应等于另一台电脑 `git rev-parse origin/cx/ai-career-rpg-home`；不要依赖文档里的旧 hash。
- 另一台电脑可以完整拉取当前阶段成果继续开发；不要只 clone 后使用默认分支，因为远端默认 HEAD 仍不是本工作分支。
- 当前不建议直接合并：完整 `npm run verify` 已通过，但全站最终视觉终审、PR 审查和真人试玩还没完成。
- 如果时间紧，可以先以当前分支继续开发或开 Draft PR 备份审查；不要把它当最终可发布主线直接 merge。

### 继续开发优先级

1. 回看第 1 章和第 5 章真人视角文案密度，确认新手不会被术语压住。
2. 做全站视觉与单屏节奏终审：桌面和 390px 手机都要看首屏是否知道“我在哪、要看什么、下一步点哪里”，避免用户为了理解两个信息点反复上下滑；实战页已经先把三张导读卡收进 `任务导演台`，并把流程图、路线牌、接力板折进 `辅助卷宗`，后续还要做完整 Lab 桌面/手机截图验收。
3. 继续抽检 Java/前端剩余关卡进入实战 Lab 后的材料、作答和验收页，确认不只教学入口正确；Java 第 4 关、前端第 3 关已有完整 Lab 深链证据，前端第 5 关已补 Lab 首屏接力修复和证物名人话化，后续仍要继续做完整作答提交到结案页的录入式验收。
4. Java 第 5 关和前端第 4 关的 teaching map/code tour 已分别在 R323/R322 改成岗位所有权；后续更深一轮应继续清理两者的沙盒证物文件名和材料结构。前端第 5 关的部分沙盒证物仍保留保存链路类样例，可后续继续岗位专属化。
5. 最后才进入 PR 合并审查：先写清楚这是阶段成果，不是学习效果已被真人证明的最终产品。

### 2026-07-15 R272：AI 15 章教学入口桌面/移动批量回归

- 使用隔离 API `CODE_QUEST_PORT=4331`、临时 SQLite `/tmp/code-quest-r272.sqlite` 和独立 Vite `http://127.0.0.1:5181/` 验收，未触碰正式 `5173` 用户数据。
- 首次直接跑同一 SPA 页面 hash 时发现后续章节不会重新挂载，已改为 `?audit=<chapter>#chapter-<chapter>` 强制整页加载；不要把第一次无效循环当验收证据。
- AI 第 1-15 章桌面 `1200×760` 批量抽检全部通过：每章都能直达教学序章、点击「开始闯关」、看到剧情角色、本章地点航线、任务契约、流程/术语线索、WebP 角色图；`scrollWidth === clientWidth`，暗色背景，无白色后台断层，未发现旧 PNG 场景图。
- AI 第 1-15 章移动 `390×844` 批量抽检全部通过：15/15 `scrollWidth = 390`，每章都有剧情角色、地点航线、任务契约、流程/术语线索和 WebP 角色图。
- Java 后端第 1 章「分层服务塔」桌面/390px 通过：从新用户序章进入路线大厅、选择 Java 后端、进入第 1 章主线、点击「开始闯关」后，显示分层塔守门人、请求城门、Controller/Service/Repository 等地点航线；两端无横向溢出。
- 前端工程第 1 章「组件剧场」桌面/390px 通过：从新用户序章进入路线大厅、选择前端工程、进入第 1 章主线、点击「开始闯关」后，显示状态编舞师、组件剧场入口、状态灯控台、Network 回廊等地点航线；两端无横向溢出。
- Playwright 控制台结果：Errors 0 / Warnings 0；独立浏览器已关闭，临时 `5181`/`4331` 服务已停止，正式 `5173` 服务未停止。
- 当前仍未声称完成：Java/前端第 2-5 章教学入口逐章浏览器回归、真实沙盒修复与报告回读、真人迁移效果和合并审查。

### 2026-07-15 R271：390px 新用户路线大厅移动抽检

- 使用独立 Playwright CLI 会话 `codequest-mobile-r271`，设置 `390×844` 视口打开正式 `http://127.0.0.1:5173/`；该会话从空白浏览器状态进入 `Lv.1 · 见习开发者` 序章。
- 移动端序章首屏显示「代码城失去了记忆」和「走进档案馆」，`documentElement.clientWidth = 390`、`scrollWidth = 390`、背景为暗色。
- 点击「走进档案馆」后进入档案馆记录员选择页；点击「调取现场证据」后出现选择反馈和「领取委托」，全程 `scrollWidth = 390`。
- 领取委托后进入 AI 应用开发路线大厅，移动端显示当前委托、角色、工作背景、完整交接链、成长契约和 15 章路线；抽检无白色后台断层，`scrollWidth = 390`。
- 切换 Java 后端路线后显示 5 章、首章「分层服务塔」、后续「第 2 至 5 章」，没有「第 2 至 15 章」或「面试议会」。
- 切换前端工程路线后显示 5 章、首章「组件剧场」、后续「第 2 至 5 章」，没有「第 2 至 15 章」或「面试议会」。
- Playwright 控制台结果：Errors 0 / Warnings 0；本轮独立浏览器会话已关闭，没有提交、推送或合并。
- 当前仍未声称完成：25 个关卡逐章移动端视觉回归、真实沙盒修复与报告回读、真人迁移效果和合并审查。

### 2026-07-15 R270：正式复访路线浏览器抽检

- 正式 `http://127.0.0.1:5173/` 当前用户为 `Lv.16 · AI 应用工程师`、`15/15`，入口显示终章归城和终章答辩官；这只能证明复访路径，不得冒充首次新手路径。
- 职业档案桌面宽度 `1280px` 下 `scrollWidth === clientWidth`，背景为暗色，抽检 DOM 中未发现白色后台断层。
- 切换 Java 后端路线后显示 5 章、首章「分层服务塔」、后续「第 2 至 5 章」，没有出现「第 2 至 15 章」或「面试议会」。
- 切换前端工程路线后显示 5 章、首章「组件剧场」、后续「第 2 至 5 章」，没有出现「第 2 至 15 章」或「面试议会」。
- 浏览器控制台 error 为 0；本轮没有重置学习记录、没有新开隔离数据库、没有提交或推送。
- 当前仍未声称完成：移动端全路线视觉抽检、三条路线逐章浏览器回归、真实沙盒修复与报告回读、真人迁移效果和合并审查。

### 2026-07-15 R269：旧资源断层防回退门禁

- 新增运行时资源契约测试，读取 `src/App.tsx`、`src/TeachingBridge.tsx` 和 `src/transferRetests.ts`，防止旧风格角色/宠物 SVG 与旧 PNG 场景重新被导入。
- 校准测试桩与交接文档中的旧 PNG 引用，当前运行时不再引用旧剧情场景 PNG 或旧角色/宠物 SVG；装备图标 SVG 不属于本次限制范围。
- Node `v24.13.1` 下 `npm run verify:quick` 通过：9 个测试文件 / 167 个测试。
- Node `v24.13.1` 下完整 `npm run verify` 通过：Prettier、ESLint、TypeScript、9 个测试文件 / 167 个测试、生产构建和 `TeachingBridge` 懒加载 chunk 检查。构建仍有主包大于 500kB 的 Vite 警告，这是已知体积债务，不是失败。
- 当前仍未声称完成：三条路线逐章浏览器视觉抽检、真实沙盒修复与报告回读、真人迁移效果和合并审查。

### 2026-07-15 R215：新项目章节 ID 与剧情注册表统一

- `src/scenarioIds.ts` 集中维护新项目 canonical ID 到旧教学 alias 的兼容映射，避免旧的本地教学进度失效。
- `chapterCinematics.ts`、`TeachingBridge.tsx` 的镜头、地图、地点场景、伙伴和故事进度均经过同一适配层；第 2–15 章不再因 ID 不匹配回退到第 1 章档案馆。
- 新增镜头与场景集合回归测试；Node 24 下完整 `npm run verify` 通过，9 个测试文件 / 164 个测试、生产构建、懒加载检查通过。
- 浏览器隔离首屏已确认可正常启动；完整逐章浏览器回归、真人学习效果、移动端全路线视觉抽检和分支合并审查仍未完成。

### 2026-07-14 R174：第五章 RAG 索引并发延迟迁移复测（最新）

### 2026-07-14 R210：TeachingBridge 按需加载复核

- `src/App.tsx` 使用 `React.lazy(() => import("./TeachingBridge"))`，并由暗色 `Suspense` 加载画面承接进入章节时的网络等待；首页不应静态导入完整教学桥。
- `npm run verify` 通过：Prettier、ESLint、TypeScript、9 个测试文件 / 151 项测试、Vite 构建和 `scripts/verify-lazy-chunks.mjs` 动态入口门禁均通过。
- 本次构建产物确认：主 JS `663.54kB`、独立 `TeachingBridge` chunk `316.59kB`、独立 `TransferRetestLab` chunk `8.17kB`。主包体积警告仍是已知债务，不得把警告误报成失败。
- 真实浏览器复核：首页当前账号已完成 15/15，所以只显示“终章归城”，不能用它冒充首次教学路径；首页到路线档案可正常打开，390px 下 `scrollWidth = clientWidth = 390`，浏览器错误日志为空。
- 当前账号学习记录没有被本次验收重置或污染。后续若需验证首次进入章节，必须先导出并恢复用户记录，或使用独立验收数据空间。

### 2026-07-15 R211：代码导读逐行巡读

- `GuidedCodeTour` 默认只渲染当前代码行和对应的人话/交接单；通过“上一行 / 下一行”推进，完整文件仍是可选展开资料。
- 这样处理是为了降低新手一次读取“流程、代码、证据、解释”时的认知负担，同时保留输入、输出和不能证明什么的证据边界。
- Node 24 下 `npm run verify` 通过：9 个测试文件 / 152 项测试、生产构建和 TeachingBridge 动态 chunk 门禁；主 JS `663.68kB`、教学 chunk `317.16kB`、复测 chunk `8.17kB`。
- 当前分支仍未提交、未推送、未合并；真人首次学习路径和学习效果仍需独立数据空间验证。

### 2026-07-15 R212：首次学习验收隔离入口

- `server/index.ts` 新增 `CODE_QUEST_PORT`，`vite.config.ts` 新增 `CODE_QUEST_API_TARGET`；不设置时仍使用正式开发默认的 `4317` API 端口。
- 这两个变量只服务本地验收和并行预览，目的是让新用户使用临时 SQLite 与独立 origin，不触碰用户已有学习记录。
- 独立首次学习验收已完成：临时 SQLite + `http://127.0.0.1:5174/` 空白 origin 从 Lv.1 序章进入“证据优先”选择、领取委托、第 1 章主线入口，并看到 15 章成长地图与“谁把什么交给谁”的流程提示；正式用户数据未被重置或污染。
- Node 24 下 `npm run verify` 通过：Prettier、ESLint、TypeScript、9 个测试文件 / 152 项测试、Vite 构建和 TeachingBridge 动态 chunk 门禁。
- 不要把临时端口配置当成生产部署要求；默认端口和代理目标仍是 `4317`。

### 2026-07-15 R262：首章实战结案页正式浏览器复核

- 正式浏览器从已有首章教学结算进入实战，复核了「进入实战练习」到成长档案结案页的连续路径。
- 结案页明确区分「已证明：代码产生真实数据库副作用」「已记录：排查路径、提示次数与解释」和「仍待证明：换一个项目场景独立迁移」，没有把剧情完成误报为能力掌握。
- 结案页保留工作复盘、Agent 委托、面试讲法和提示等级，首章实战后的成长记录可继续回到路线图。
- 仍未声称完成：空白用户手动修复首关沙盒并读取通过报告、三条路线逐章浏览器视觉抽检、真人迁移效果和分支合并审查。

### 2026-07-15 R263：混合岗位章节进度排序修复

- `careerProfile` 不再用 `Number(chapterId)` 排序所有章节；带岗位前缀的 `java-1`、`frontend-2` 现在与 AI 数字章节保持确定性顺序。
- 新增回归测试，锁定混合岗位章节写入成长档案时不会因为 `NaN` 比较导致收集顺序漂移；快速门禁通过 9 个测试文件 / 164 个测试。

### 2026-07-15 R264：三条岗位路线入口浏览器复核

- 在隔离 API、临时 SQLite 和独立 Vite origin 中，从空白用户走过序章「调取现场证据」→领取委托→岗位路线选择，逐一切换 AI 应用开发、Java 后端和前端工程。
- AI 入口显示 15 章「数据断层」；Java 入口显示 5 章「分层服务塔」，目标为追踪 Controller → Service → Repository → 数据库；前端入口显示 5 章「组件剧场」，目标为追踪用户操作 → 状态所有者 → 组件重渲染。
- 三条路线的伙伴、地点、流程、产出和地图数量均随路线变化，没有回退到 AI 默认第一关。隔离 API/SQLite 已停止，正式 `5173` 服务未停止。
- 当前状态：入口路线验收完成，但逐章浏览器视觉抽检、用户真实沙盒修复和跨项目迁移学习效果仍未完成。

### 2026-07-15 R213：线索回声回看

- `EvidenceStoryQuest` 的已收录线索不再禁用；按钮会显示“卷宗已收录 · 点击回看”，重新打开时不会重复计数或影响通关条件。
- 这项改动针对真实新手认知负担：进入下一地点后仍能回看上一幕的结果、代码片段和“能证明 / 不能证明”边界，不要求靠记忆补流程。
- 新增前端回归测试，完成后需要继续跑 `npm run verify`。

- 第 5 章数据一致性实战完成至少 24 小时后，会解锁“双星索引井”：前端只有一次知识文档上传，两名索引 Worker 却在同一竞争窗口读到 `pending`，各自写入一套重复 chunks。
- 新增 `sandbox/rag-index-concurrency-retest` 独立沙盒与种子证物。初始测试稳定为 2 passed / 1 failed，失败用例证明“先 SELECT 再 UPDATE”会让两个 Worker 同时领取同一任务；修复训练要求区分原子领取、业务唯一约束和事务边界。
- 新增 `scene-twin-star-indexing-well.webp` 与 `portrait-index-arbiter.webp`。第五份复测使用上下双 Worker 轨道汇入唯一索引库的 `worker-confluence` 地图，以及先展示两路、再绕向汇流点的 `split-orbit` 镜头，不复用前四章构图。
- 参考 Unity、Epic、GDC 与 Ubisoft 的公开资料补强 `docs/game-camera-and-map-rules.md`：关卡需要可辨认地标、首屏空间关系和与知识结构一致的路线拓扑，不能只换背景或颜色。
- 浏览器桌面 1280×720 与手机 390×844 验收通过：双轨地图、证物标签和页面均无横向溢出，证物切换正常，控制台无错误；手机首屏能同时看到导师、案件、完整汇流图和当前任务。
- 验收前备份学习记录，临时把第 5 章改为 25 小时前提交并启动复测；验收后覆盖恢复。忽略 `exportedAt` 后恢复前后 SHA-1 均为 `162b6233d0590c9b350f8e4dd5ee17e5ab7e1028`，第 5 章恢复为 `active`，临时复测 attempt 已删除。
- Node 24 全量门禁通过：Prettier、ESLint、TypeScript、9 个测试文件 / 132 项测试和 Vite 构建。主 JS `626.52kB`、教学 chunk `310.46kB`、延迟复测 chunk `8.16kB`；主包体积警告仍是已知债务。
- 当前延迟复测覆盖第 1–5 章；第 6–15 章和真人迁移效果仍未完成。

### 2026-07-14 R176：第七章模型密钥轮换延迟迁移复测

- 第 7 章 AI API 实战完成至少 24 小时后，会解锁“模型供应商换钥后助手失声”；新案件使用 `sandbox/model-key-rotation-retest`，要求区分浏览器请求、自有 API、服务端环境变量、模型供应商和安全错误兜底。
- 新增模型密钥熔炉配置、独立证物、密钥边界地图和按钥匙匣推进的镜头；初始沙盒报告稳定为 2 passed / 1 failed，失败点是网关仍把旧 Key 写死，不能随环境轮换。
- `src/transferRetests.ts` 与服务端白名单已接入第 7 章；供应商 401 应被转换为不泄露内部配置的可行动错误。
- 第 6 章 `ai-briefing-latency-retest` 已在当前工作树真实接入，本轮同步校准了之前仍写“只覆盖前 5 章”的旧交接文案。
- 定向测试 39 条通过；Node 24 下沙盒失败报告符合教学预期。完整 `npm run verify` 通过（9 个测试文件、136 项测试），桌面/390px 入口无横向溢出、控制台 0 error；截图位于 `output/playwright/r176-chapter7-desktop.png` 和 `r176-chapter7-mobile-390.png`。
- 浏览器验收创建的临时第 7 章教学 attempt 已从本地 SQLite 清理，`ai-api-key-vault` 来源仍保持未开始，不污染用户学习进度。

### 2026-07-14 R177：第八章引用可验证回答延迟迁移复测

- 第 8 章幻觉控制实战完成至少 24 小时后，会解锁“客服回答听起来对，却引用了不存在的退款条款”；新案件使用 `sandbox/citation-grounding-retest`，要求把模型语气、资料上下文、Prompt 合约和引用命中分开判断。
- 新增幻觉镜厅、镜厅校对师、引用镜面地图和镜面横扫镜头；初始沙盒报告稳定为 2 passed / 1 failed，失败点是资料未命中时仍然补全事实，没有安全拒答边界。
- `src/transferRetests.ts` 与服务端白名单已接入第 8 章，证物包括模型回答、允许引用的退款资料、Prompt 契约、引用校验和日志。
- Node 24 下完整 `npm run verify` 通过：9 个测试文件、138 项测试；桌面 1200×760 与手机 390×844 均无横向溢出、控制台 0 error，手机主操作底部约 800px。截图位于 `output/playwright/r177-chapter8-direct-desktop.png` 和 `r177-chapter8-direct-mobile-390.png`。
- 浏览器使用已有第 8 章来源 attempt 复核，没有创建新的复测 attempt；`citation-grounding-retest` 仍未开始。

### 2026-07-14 R178：第九章 RAG 检索版本错配延迟迁移复测

- 第 9 章 RAG 实战完成至少 24 小时后，会解锁“资料在库里，但 AI 引用了旧版本规则”；新案件使用 `sandbox/retrieval-mismatch-retest`，要求追踪文档、Chunk、Embedding、TopK 命中和最终引用。
- 新增知识迷宫深井、检索狐、检索迷宫地图和迷宫环绕镜头；初始沙盒报告稳定为 2 passed / 1 failed，失败点是检索器只按相似度排序，没有优先当前生效版本。
- `src/transferRetests.ts` 与服务端白名单已接入第 9 章，证物包括上传 Network、Chunk 来源、TopK 命中、检索器代码、错误回答和日志。
- Node 24 下完整 `npm run verify` 通过：9 个测试文件、140 项测试；桌面 1200×760 与手机 390×844 均无横向溢出、控制台 0 error，手机主操作底部约 775px。截图位于 `output/playwright/r178-chapter9-desktop.png` 和 `r178-chapter9-mobile-390.png`。
- 浏览器使用已有第 9 章来源 attempt 复核，没有创建新的复测 attempt；`retrieval-mismatch-retest` 仍未开始。

### 2026-07-14 R179：第十章 Agent 工具边界延迟迁移复测

- 第 10 章 Agent 工具实战完成至少 24 小时后，会解锁“订单查询请求混入内部路径”的新案件；复测要求区分 Agent 意图、工具注册表、参数 Schema、资源权限和审计回退。
- 新增 `sandbox/tool-boundary-retest`，初始报告稳定为 2 passed / 1 failed；失败点是当前实现只有明显路径前缀拦截，没有通用资源归属/租户权限校验，不能把一次拒绝冒充完整修复。
- 新增工具契约高塔、塔楼副官、可爱机械守门伙伴、权限门高塔地图和上升镜头；手机端五节点改为两列，末节点整行显示。
- 服务端白名单、延迟门禁、必填步骤和材料路径脱敏已覆盖；Node 24 下完整 `npm run verify` 通过：9 个测试文件、141 项测试；第十章桌面/390px 浏览器无横向溢出、控制台 0 error，截图位于 `output/playwright/r179-chapter10-desktop.png` 和 `r179-chapter10-mobile-390.png`。
- 当前数据库中第十章原始实战尚未提交，API 正确返回 `prerequisite`；未创建演示用复测 attempt，避免污染真实学习记录。

### 2026-07-14 R180：第十一章可信验收证据延迟迁移复测

- 第 11 章验收试炼完成至少 24 小时后，会解锁“全部通过但绑定旧源码”的新案件；复测要求区分失败复现、单测、集成测试、手动路径、Network 和源码指纹。
- 新增 `sandbox/verification-proof-retest`，初始报告稳定为 2 passed / 1 failed；失败点是校验器只看全绿结果，没有拒绝旧源码报告，训练用户不把绿色数字当成可信交付。
- 新增验收试炼场、验收试炼官、竞技场下落镜头和证据竞技场地图；五个流程节点在手机端改为两列并保持源码指纹整行展示。
- 服务端白名单、延迟门禁、必填步骤和材料路径脱敏已覆盖；Node 24 下完整 `npm run verify` 通过：9 个测试文件、143 项测试；第十一章复测桌面/390px 无横向溢出、控制台 0 error。截图位于 `output/playwright/r180-proof-retest-desktop.png` 和 `output/playwright/r180-proof-retest-mobile-390.png`。
- 浏览器验收使用临时第十一章来源和复测 attempt，完成后已删除两条临时记录；API 恢复为 `prerequisite`，没有改变真实学习记录。

### 2026-07-14 R181：第十二章 Agent 委托契约延迟迁移复测

- 第 12 章 Agent 委托实战完成至少 24 小时后，会解锁“批量导入委托缺少边界和回滚”的新案件；复测要求区分业务背景、可观察目标、允许范围、验收标准和风险回退。
- 新增 `sandbox/brief-contract-retest`，初始报告稳定为 2 passed / 1 failed；失败点是契约校验器没有强制回滚方案，训练用户识别“能执行”与“能安全交付”的差别。
- 新增委托书锻造工坊、委托书锻造师、契约展开镜头和 Agent 委托契约地图；五个流程节点在手机端改为两列，风险回滚整行展示。
- 服务端白名单、延迟门禁、必填步骤和材料路径脱敏已覆盖；Node 24 下完整 `npm run verify` 通过：9 个测试文件、145 项测试；第十二章复测桌面/390px 无横向溢出、控制台 0 error，截图位于 `output/playwright/r181-brief-retest-desktop.png` 和 `output/playwright/r181-brief-retest-mobile-390.png`。

### 2026-07-14 R182：第十三章交付证据审查延迟迁移复测

- 第 13 章交付审查实战完成至少 24 小时后，会解锁“Diff 很小但测试、移动端和文档证据都不完整”的新案件；复测要求对照交付说明、Diff 范围、测试源码版本、390px 浏览器和文档同步。
- 新增 `sandbox/review-evidence-retest`，初始报告稳定为 2 passed / 1 failed；失败点是审查器没有拒绝过期测试证据，训练用户不把小 Diff 或绿色 CI 直接等同于可合并。
- 新增交付审查庭、交付审查官、菱形扫镜头和审查庭地图；五个流程节点在手机端改为两列，审查决定整行展示。
- 服务端白名单、延迟门禁、必填步骤和材料路径脱敏已覆盖；Node 24 下完整 `npm run verify` 通过：9 个测试文件、147 项测试；第十三章复测桌面/390px 无横向溢出、控制台 0 error，截图位于 `output/playwright/r182-review-retest-desktop.png` 和 `output/playwright/r182-review-retest-mobile-390.png`。

### 2026-07-14 R183：第十四章上线门禁延迟迁移复测

- 第 14 章上线前夜实战完成至少 24 小时后，会解锁“环境变量齐全但恢复、移动端冒烟和监控没有闭环”的新案件；复测要求判断负责人、配置、备份恢复、冒烟、监控和回滚是否形成完整门禁。
- 新增 `sandbox/release-proof-retest`，初始报告稳定为 2 passed / 1 failed；失败点是上线校验器忽略备份恢复验证，训练用户区分“备份存在”和“事故后真的回得来”。
- 新增上线门禁塔、上线守门人、门锁镜头和上线安全地图；五个流程节点在手机端改为两列，回滚决定整行展示。
- 服务端白名单、延迟门禁、必填步骤和材料路径脱敏已覆盖；Node 24 下完整验证与第十四章桌面/390px 浏览器验收待本次收尾后补录。

### 2026-07-14 R184：第十五章面试证据延迟迁移复测

- 第 15 章终章答辩实战完成至少 24 小时后，会解锁“换一份 AI 知识库事故也要讲清证据”的新案件；复测要求把素材、STAR、故障复盘、技术取舍和追问连成可信表达，并明确未验证边界。
- 新增 `sandbox/interview-proof-retest`，初始报告稳定为 2 passed / 1 failed；失败点是答辩校验器没有拦截“完全掌握所有类似问题”的夸大表述，训练用户区分工程证据与自我评价。
- 新增终章答辩厅、终章答辩官、答辩环绕镜头和面试证据星图；五个流程节点在手机端改为两列，追问应答整行展示。
- 服务端白名单、延迟门禁、必填步骤和材料路径脱敏已覆盖；Node 24 下完整 `npm run verify` 通过：9 个测试文件、151 项测试；第十五章复测桌面/390px 无横向溢出、控制台 0 error，截图位于 `output/playwright/r184-interview-retest-desktop.png` 和 `output/playwright/r184-interview-retest-mobile-390.png`。

### 2026-07-14 R173：第四章模型限流延迟迁移复测

- 第 4 章接口错误实战完成至少 24 小时后，会解锁“模型限流被网关伪装成系统错误”：合法客服摘要批次穿过自有 API 后，上游模型返回 `429 + retry-after`，错误映射器却只向浏览器返回无关联编号的 `500`。
- 新增 `sandbox/model-rate-limit-retest` 独立沙盒，覆盖批处理页面、网关路由、模型供应商边界、浏览器请求、自有 API 响应、上游 429、关联日志、限流约定和 Agent 交付说明。种子测试稳定为 2 passed / 1 failed，唯一失败是预期可行动 `503`、实际通用 `500`。
- 新增 `scene-signal-storm-dispatch-tower.webp` 和 `portrait-storm-dispatcher.webp`：暴雨调度塔、分流管线、风暴调度官与暖金机械猫头鹰都来自同一张关键美术，场景和人物不会割裂。
- 第四份复测使用故障分流地图与告警下潜镜头；用户必须分别解释浏览器、自有 API、模型供应商和错误映射器收到什么、交出什么，再用同一 `requestId` 串起证据。
- 390×844 浏览器验收发现证物标签横向滚动会截掉第三份上游 429，已把所有复测的手机证物栏改成自动换行网格。故障树和三份证物的 `scrollWidth === clientWidth`，桌面/手机均无页面横向溢出，证物切换正常，控制台无错误。
- 验收前备份学习记录，临时把第 4 章改为 25 小时前提交并启动复测；验收后覆盖恢复。忽略 `exportedAt` 后恢复前后 SHA-256 均为 `b60a0455a214be51f3a1b11f55cddf49f33f100f1110f0f37dfc92aa545d69e0`，第 4 章恢复为 `active`，临时复测 attempt 已删除。
- Node 24 全量门禁通过：Prettier、ESLint、TypeScript、9 个测试文件 / 130 项测试和 Vite 构建。主 JS `622.91kB`、教学 chunk `310.46kB`、延迟复测 chunk `8.16kB`；主包体积警告仍是已知债务。
- 当前延迟复测覆盖第 1–7 章，第 8–15 章与真人迁移效果仍未完成。

### 2026-07-14 R172：第三章客服夜班延迟迁移复测（最新）

- 第 3 章登录态实战完成至少 24 小时后，会解锁独立业务“认证服务重启后夜班客服集体掉线”；它使用 `sandbox/support-shift-session-retest`，不复用原身份回廊的文件、答案或测试。
- 新案件要求用户独立判断登录 200、浏览器 Cookie、续期 401 和 SQLite 会话记录各自能证明什么，再完成证据计划、Agent 委托和人工运行测试后的迁移复盘。
- `src/transferRetests.ts` 新增夜航身份中转站、夜航身份官、会话回环地图与检查点跟拍镜头；服务端注册 9 份白名单证物并继续执行 24 小时门禁。
- 390×844 浏览器验收发现会话地图内部横向拖动后，已改成两列五节点布局；地图 `scrollWidth === clientWidth === 346px`，页面横向溢出为 0，控制台无错误。桌面端三张复测卷宗均可见且视觉一致。
- 验收前备份本地学习记录，临时把第 3 章改为 25 小时前提交并启动复测；验收后覆盖恢复。忽略 `exportedAt` 后恢复前后 SHA-256 均为 `280bf356882652938b7106c210ef110fb4aa4781abe96a23aea26f4afb4cecbb`，第 3 章恢复为 `active`，临时复测 attempt 已删除。
- Node 24 全量门禁通过：Prettier、ESLint、TypeScript、9 个测试文件 / 128 项测试和 Vite 构建。主 JS `619.22kB`、教学 chunk `310.46kB`、延迟复测 chunk `8.16kB`；主包体积警告仍是已知债务。
- 当前只完成第 1–3 章延迟复测，第 4–15 章仍未开发；真人是否能独立迁移登录态知识也未验证，不得写成“已经掌握”。

### 2026-07-14 R171：十五章镜头节奏与地图视觉分化（最新）

- `src/chapterCinematics.ts` 在原有十五种镜头序列和地图拓扑上，新增每章专属镜头时长、过渡曲线、地貌、关键地标、强调色与柔光色；地图差异不再只靠节点坐标。
- `TeachingBridge` 会把章节镜头时长/easing 传入剧情背景，并在地图标题直接显示区域地貌和关键地标；地图步骤的导师与伙伴改为紧凑模式，1280×720 和 390×844 首屏都能露出第一个可操作节点。
- `styles.css` 为十五种拓扑增加不同的底图纹理、节点轮廓和路径虚线节奏。浏览器抽检产品取舍工作台、性能瀑布和职业证据星图，分别呈现工坊横纹、时间刻度、星点底图，节点和路径也不同；两端横向溢出为 0，控制台无错误。
- 浏览器验收时系统启用了 `prefers-reduced-motion`，所以实际动画被关闭；第 13 章 DOM 仍确认读取专属 `11s` 和反打曲线，说明动效契约与减弱动效边界同时生效。
- 验收前导出完整学习备份，临时调整第 2/6/13/15 章教学进度；验收后覆盖恢复为 15 个 attempts / 94 条 teaching_progress，恢复前后 `.tables` SHA-256 均为 `0900086930c3098ad27474df2f2590bd3a350c989c0d94417fd24083cbdb9161`。
- 最终 Node 24 `npm run verify` 通过：Prettier、ESLint、TypeScript、9 个测试文件 / 126 项测试和 Vite 构建；主 JS `615.77kB`，按需教学 chunk `310.46kB`，延迟复测 chunk `8.16kB`，主包大于 500kB 的警告仍是已知债务。
- 研究与实现规则见 `docs/game-camera-and-map-rules.md`。该轮只能证明镜头/地图差异可见和界面可用，不能证明真人因此更容易理解或更愿意继续玩。

### 2026-07-14 R170：十五章冒险等级与伙伴成长循环（最新）

- `src/careerProfile.ts` 现在统一管理章节 XP、Lv.1–16、八段冒险阶位和旧档补分。第 1 章 `150 XP`，第 2–15 章各 `120 XP`；十五章首次通过合计 `1830 XP`，对应 `Lv.16 / AI 应用工程师`。
- 旧版本已通关但少发 XP 的档案会按 `clearedChapterIds` 补足最低应得 XP，只补不扣；重复通关继续不重复发 XP 或解锁伙伴。用户原 15/15 档案已在正常页面加载时从 `1170 XP` 补到 `1830 XP`。
- 全局角色 HUD、归城记录、当前委托成长契约和章节会合/结算已显示等级星轨；实战通过跨等级或阶位时显示晋升反馈，并明确游戏称号不等于能力认证。
- 修复结算转场继承上一页滚动位置的问题；实战前会合页在 1280×720 和 390×844 都是整屏高度，角色、等级条、三项门槛、主按钮和返回按钮无需滚动，横向溢出为 0，浏览器控制台无错误。
- 最终 `npm run verify` 通过：Prettier、ESLint、TypeScript、9 个测试文件 / 126 项测试和 Vite 构建。主 JS `615.77kB`，教学 chunk `307.11kB`，延迟复测 chunk `8.16kB`；主包体积警告仍存在。
- 成长规则详见 `docs/adventure-progression-rules.md`。后续不得把最高阶位退回 600 XP，也不得在剧情教学完成时提前发奖。

### 2026-07-14 R169：十五章专属运镜与地图（最新）

- `src/chapterCinematics.ts` 是 15 章镜头、焦点、地图名称、说明和拓扑的唯一事实源；`src/chapterCinematics.test.ts` 守住 15 章完整性、拓扑唯一性、坐标边界、地点换镜和第 13 章真实分流。
- `EvidenceStoryQuest` 与章节序章按 `scenarioId + sceneIndex` 切换景别；`ProjectMapView` 使用十二列拓扑与真实 `projectMap.edges`，手机端回落为单列。
- 浏览器抽检第 2 章产品取舍工作台、第 9 章 RAG 检索迷宫、第 13 章交付审查菱形：1280 与 390px 均无横向溢出，控制台无错误。第 13 章在浏览器验收中发现“视觉分叉、数据线性”后已修成真实双路汇合。
- 为查看已通关章节，验收前导出本地学习记录，临时重置三章教学状态，验收后通过覆盖导入恢复；恢复前后 `.tables` SHA-256 都是 `3f2500da44f73b840dc9db393d306611eb67d18bab9906ed1b00f98d19cb1b8d`。
- 最终 `npm run verify` 通过：Prettier、ESLint、TypeScript、9 个测试文件 / 123 项测试和 Vite 生产构建。主 JS `613.51kB`，按需教学 chunk `307.11kB`，延迟复测 chunk `8.16kB`；主包体积警告仍是已知债务。
- 当前页面已恢复到 `http://127.0.0.1:5173/`，用户学习记录未被验收状态污染。真人是否能借地图更快理解仍未验证，不得写成学习效果通过。

## 分支与合并状态（2026-07-12）

- 当前继续开发分支：`cx/ai-career-rpg-home`
- 当前已上传提交：`09c3228 feat(rpg): expand guided career adventure`
- 远端位置：`origin/cx/ai-career-rpg-home`
- 远端仓库当前会提示迁移到 `https://github.com/xixinikl/code-quest.git`，但本地 `origin` 仍指向 `https://github.com/2082743849-beep/code-quest.git` 并可正常推送。
- 旧引导式教学分支：`origin/feat/guided-learning-bridge`
- 旧分支当前远端最新提交：`10132c1 chore: onboard project to Xixi Dev System (#1)`
- 本分支已作为阶段成果推送，但尚未准备直接合并到主线；建议先开 PR 审查，不要直接 merge。原因：改动量极大，真人学习效果、真实沙盒修复和逐章回归仍未全部完成。
- 后续合并前先读：`docs/branch-merge-status-2026-07-12.md`

### 2026-07-16 当前接手须知

- 当前大版本阶段成果已提交并推送：`09c3228 feat(rpg): expand guided career adventure`。接手前仍要先跑 `git status --short --branch`，确认是否有后续本地补丁。
- 本次收口前完整 `npm run verify` 已通过：格式、Lint、类型、10 个测试文件 / 170 个测试、生产构建和 TeachingBridge 懒加载检查。
- 不要把“已推送分支”理解成“已合并 main”。合并前仍需 PR 审查、用户视觉确认和关键路径浏览器抽检。
- 最新本地修复：任务简报右侧“选中章节卷宗”默认跟随下一章主线。新号默认显示第 1 章卷宗；通关第 1 章后默认显示第 2 章卷宗；用户手动点第 3-15 章时才切换到对应卷宗。
- 这个修复解决用户反馈的“左边当前委托说一章，右边卷宗却显示别章”的认知断层；后续不要把 `selectedChapterId` 默认值改回固定 `"3"`。
- 已验证：`npm run verify` 通过，包含 Prettier、ESLint、TypeScript、64 个 Vitest 测试和 Vite 生产构建。
- 浏览器已打开 `http://127.0.0.1:5173/#chapter-2` 验证页面可加载；当前本地教学记录已完成第 2 章，因此进入结算前夜，没有为了查看按钮而清空用户学习记录。

**已完成：**

- 引导式教学桥（项目地图 → 4概念卡 → 代码导读 → 共同示范 → 陪练清单）
- AI 应用开发职业路线入口（默认选中 AI 应用开发，展示主线任务地图）
- 两个主线任务：主线 1-1（保存消失）、主线 1-2（CanvasStorm 产品链路教学）
- AI 应用开发 15 章长期主线卷轴：数据排障、产品链路、登录态、接口错误、数据一致性、性能、AI API、幻觉控制、RAG、Agent、测试、任务、交付审查、上线与面试复盘
- `src/careerRoadmap.ts` 已作为 AI 路线 manifest，首页从这里读取路线
- `src/careerRoadmap.ts` 已增加 `careerRoutes` 多岗位路线骨架：首页显示 AI 应用开发可进入，Java 后端和前端工程为“即将解锁”规划路线
- 任务简报支持点击后续章节查看九件套卷宗；第 3-15 章已有完整学习契约预览和剧情教学入口
- 第 2 章“AI 点子为什么空泛”已经能从路线卷宗直接进入 CanvasStorm 产品链路剧情教学；剧情探索后继续进入项目地图、概念和代码导读，不直接结算；底层教学数据已改为 Project Brief、方向筛选、候选取舍和会话保存；登录态 Token/Cookie 内容保留在第 3 章
- 第 2 章已有 `sandbox/canvasstorm-product-brief` 种子沙盒和服务端白名单材料，覆盖 Brief、方向筛选、候选取舍和会话保存；并已接入专属实战 Lab，显示本章步骤、提示、材料导览、沙盒目录和产品链路复盘
- 第 3 章已有 `sandbox/identity-session-corridor` 种子沙盒和服务端白名单材料，覆盖登录响应、Cookie/Token 持久化、401 Network、刷新恢复和 Agent 交付证据；并已接入专属实战 Lab，显示身份路线、凭证存放、401 反证、材料导览、沙盒目录和登录态面试复盘
- 第 4 章已有 `sandbox/api-error-court` 种子沙盒和服务端白名单材料，覆盖请求体、状态码、结构化错误、后端日志和 Agent 交付证据；并已接入专属实战 Lab，显示请求体检查、状态码判定、错误结构、日志串证、材料导览、沙盒目录和接口排障复盘
- 第 5 章已有 `sandbox/data-consistency-forge` 种子沙盒和服务端白名单材料，覆盖重复提交、Idempotency-Key、唯一约束、事务边界、数据库数量和 Agent 交付证据；并已接入专属实战 Lab，显示连点现场、幂等键、数据库数量、事务边界、材料导览、沙盒目录和数据一致性复盘
- 第 6 章已有 `sandbox/performance-fog-lab` 种子沙盒和服务端白名单材料，覆盖 Network 瀑布图、Server-Timing、前端渲染画像、缓存复测和 Agent 交付证据；并已接入专属实战 Lab，显示瀑布图阅读、后端计时、渲染画像、缓存复测、材料导览、沙盒目录和性能瓶颈复盘
- 第 7 章已有 `sandbox/ai-api-key-vault` 种子沙盒和服务端白名单材料，覆盖前端密钥扫描、服务端环境变量、流式响应、上游失败兜底和 Agent 交付证据；并已接入专属实战 Lab，显示密钥泄露、服务端 key、流式响应、失败兜底、材料导览、沙盒目录和 AI API 安全复盘
- 第 8 章已有 `sandbox/hallucination-mirror-hall` 种子沙盒和服务端白名单材料，覆盖 Prompt 约束、上下文资料、编造引用、无资料硬答、引用校验和 Agent 交付证据；并已接入专属实战 Lab，显示 Prompt 合约、上下文资料、编造引用、无资料拒答、材料导览、沙盒目录和幻觉控制复盘
- 第 9 章已有 `sandbox/rag-knowledge-maze` 种子沙盒和服务端白名单材料，覆盖 chunk 索引、来源路径、topK 命中错误、回答引用缺失、无关问题拒答和 Agent 交付证据；并已接入专属实战 Lab，显示 chunk 来源、检索错配、matches/citations、未命中拒答、材料导览、沙盒目录和 RAG 面试复盘
- 第 10 章已有 `sandbox/agent-tool-tower` 种子沙盒和服务端白名单材料，覆盖工具注册表、参数 schema、越权调用、失败回退、审计日志和 Agent 交付证据；并已接入专属实战 Lab，显示工具注册表、参数 schema、权限门禁、失败回退、审计日志、材料导览、沙盒目录和 Agent 工具复盘
- 第 11 章已有 `sandbox/verification-trial-arena` 种子沙盒和服务端白名单材料，覆盖失败复现、单元测试、集成测试、手动复测、过期报告和回归风险；并已接入专属实战 Lab，显示失败复现、单元边界、集成流程、手动复测、过期报告、回归风险、材料导览、沙盒目录和可信验收复盘
- 第 12 章已有 `sandbox/agent-brief-forge` 种子沙盒和服务端白名单材料，覆盖空泛委托、越界委托、清晰委托、验收命令、浏览器路径、风险回滚和 Agent 交付格式；并已接入专属实战 Lab，显示空泛委托、可观察目标、安全边界、验收路径、风险回滚、交付格式、材料导览、沙盒目录和 Agent 委托面试复盘
- 第 13 章已有 `sandbox/delivery-review-court` 种子沙盒和服务端白名单材料，覆盖交付说明、Diff 范围、过期测试证据、移动端验收缺口、文档同步缺口和拒收决定；并已接入专属实战 Lab，显示交付说明核对、Diff 范围、过期测试、移动端缺口、文档同步、拒收决定、材料导览、沙盒目录和 Agent 交付审查复盘
- 第 14 章已有 `sandbox/release-readiness-gate` 种子沙盒和服务端白名单材料，覆盖上线计划、生产环境变量、备份恢复、移动端冒烟、监控信号和回滚方案；并已接入专属实战 Lab，显示上线计划、生产配置、备份恢复、桌面/390px 冒烟、监控信号、回滚退路、Agent 补证任务和上线面试复盘
- 第 15 章已有 `sandbox/interview-answer-forge` 种子沙盒和服务端白名单材料，覆盖项目素材库、STAR 草稿、故障复盘、技术取舍、追问演练和答辩 Rubric；并已接入专属实战 Lab，显示项目素材库、STAR 草稿、故障复盘、技术取舍、追问演练、答辩 Rubric、Agent 面试官、沙盒目录和终章面试复盘
- 教学桥后半段已统一为暗色 RPG 风格；代码导读页有阅读罗盘、流程梯、逐行翻译、证据边界和 Agent 交接话术
- 教学桥进度只统计当前章节步骤，历史剧情记录不会把进度撑到 100% 以上
- 第 3 章“登录状态为什么丢”已经能从卷宗进入身份回廊剧情教学，包含 Cookie、Token、Session、401 和刷新掉登录的流程解释
- 第 4 章“接口为什么报错”已经能从卷宗进入接口审判庭剧情教学，包含请求体、状态码、结构化错误和后端日志流程解释
- 第 5 章“数据为什么重复/错乱”已经能从卷宗进入一致性熔炉剧情教学，包含重复提交、Idempotency-Key、唯一约束、事务和数据库记录数验收解释
- 第 6 章“页面为什么慢”已经能从卷宗进入慢速迷雾剧情教学，包含 Network 瀑布图、TTFB、后端计时日志、渲染卡顿、缓存和性能复测解释
- 第 7 章“AI 接口怎么接”已经能从卷宗进入模型熔炉剧情教学，包含 API Key、环境变量、服务端转发、流式响应和错误兜底解释
- 第 8 章“AI 回复为什么胡说”已经能从卷宗进入幻觉镜厅剧情教学，包含 Prompt、上下文、引用来源、引用校验和无资料拒答解释
- 第 9 章“RAG 知识库”已经能从卷宗进入知识迷宫剧情教学，包含资料来源、chunk、embedding、topK 命中和带引用回答解释
- 第 10 章“Agent 工具调用”已经能从卷宗进入工具契约大厅剧情教学，包含工具注册表、参数 schema、权限门禁和失败回退解释
- 第 11 章“测试怎么证明修好了”已经能从卷宗进入验收试炼场剧情教学，包含复现用例、单元测试、集成测试、手动测试报告和回归风险解释
- 第 12 章“Agent 任务怎么写”已经能从卷宗进入委托书工坊剧情教学，包含背景、目标、约束、验收和风险解释
- 第 13 章“怎么审查交付”已经能从卷宗进入交付审查庭剧情教学，包含交付说明、Diff、测试证据、边界条件、文档同步和拒收理由解释
- 第 14 章“上线前检查什么”已经能从卷宗进入上线前夜剧情教学，包含上线计划、环境变量、数据备份、监控哨塔、回滚条件和冒烟验收解释
- 第 15 章“面试怎么讲项目”已经能从卷宗进入终章答辩厅剧情教学，包含 STAR、故障复盘、技术取舍、成长证据和追问演练解释
- 每章路线 manifest 已增加伙伴/宠物/装备解锁物，首页任务卡和卷宗会展示可收集奖励
- 首页任务简报已增加“伙伴图鉴”，展示 15 章可收集角色、宠物、装备及收集状态
- 首页任务简报已增加“伙伴背包/收藏册”，支持全部、已收集、可获取筛选，并展示能力印记、工作场景和面试复盘
- 首页任务简报已增加“面试复盘册”，把章节素材整理成现象、定位证据、行动/修改、验证动作和可迁移经验
- 首页任务简报已增加独立“面试复盘房间”，支持选择章节、填写五段复盘草稿，并通过 `interview-dossier` step 保存到本地 SQLite 学习记录
- 首页任务简报已增加“面试作品集”出口，可把 15 章路线素材和用户复盘草稿整理成可复制 Markdown，并明确学习项目边界
- 首页任务简报已增加“本地备份库”，可导出/恢复 SQLite 学习记录和成长档案 JSON，不包含真实项目源码或账号信息
- 任务简报首屏节奏已修复：左侧身份目标/伙伴/复盘入口从首屏开始，右侧 15 章路线改为可滚动任务册，桌面和 390px 手机无横向溢出
- 章节卷宗已增加剧情舞台：选中章节会切换地点背景、角色/宠物/装备图和流程接力，减少纯文字表格感
- 章节卷宗舞台已继续补强：每章有向导登场、开场对白、玩家目标和“谁把什么交给谁”的证据接力板；点击章节后自动滚到卷宗，帮助初学者先看懂流程再进入关卡
- 教学桥项目地图已增加地图向导、三步阅读顺序和“这一站的接力”解释，剧情探索结束后不会突然掉回普通节点列表
- 教学桥正文前已增加“本章记忆线”：每一步都会显示刚刚看过、当前这一棒和接下来，减少用户在地图、概念、代码和结案之间切换时忘记流程
- 教学桥概念卡已增加“术语解锁室”角色引导，把生活类比、当前项目例子和预测题串成术语解锁流程
- 教学桥代码导读已增加“代码巡读官”、三步阅读契约和证据交接提示，帮助初学者先看上一棒材料、再读关键行、最后找反证
- 代码导读逐行翻译已继续去泛化：字段清单、对象字段、回调和方法调用会给出具体解释，后续不要恢复“这一行负责继续加工材料”这类套话
- 章节结案页已增加“工作里怎么用 / 证据链怎么验 / 面试怎么讲”三格复盘，避免最后一步只给一大段文字，帮助用户整理成可复述的工作和求职表达
- 章节结案页已增加“给 Agent 的委托口令”：用背景、边界、验收三句把本章结论转成可交给 Agent 的下一步任务骨架
- 章节结案页已增加“结案导师”舞台：复用本章最后登场角色、地点和导师台词，并展示本章能力印记，避免剧情结束后突然变回普通报告页
- 教学完成页已改为“结算前夜”舞台：复用本章导师、展示已收录学习证据和下一步边界，避免章节结案到奖励结算之间出现普通庆祝页断层
- 陪练页已增加“实战前夜作战简报”：用档案馆记录员、目标、边界和验收口径把用户带入沙盒实战，避免进入真实练习前只看到普通 checklist
- 启动加载、教学材料加载和服务错误页已统一为暗色档案馆状态屏，避免刷新、加载或异常时露出浅色后台断层
- 剧情探索线索已增加“卷宗已收录”完成态，已发现线索不会继续像新探索点一样重复触发
- 剧情探索已增加“下一地点预告”：本地点线索找齐后会提示下一幕地点、登场角色和学习目标，减少换场景时的断层
- 剧情探索已增加“场景转移”字幕：点击前往下一地点后会短暂显示目的地，再切换背景和角色
- 剧情探索已增加“本幕收获”：每个地点线索找齐后会把线索技能汇总成证据印记，帮助用户知道这一幕学到了什么
- 剧情探索“本幕收获”已增加“面试一句话”，会把本幕证据线索转成可复盘表达
- 剧情探索“本幕收获”已增加“现象 / 证据 / 结论”三段式复盘，帮助初学者把一幕剧情整理成可复述的判断骨架
- 章节教学通关后会出现暗色 RPG 结算页，展示 XP、阶位变化、能力印记、伙伴/宠物/装备解锁和面试复盘
- 第 2-15 章教学完成后已接入奖励舞台：先展示解锁物、能力印记和下一章预告，再让用户选择进入本章实战或回路线图
- 主线 1-1 结案门已补上“档案馆记录员”解锁、能力印记和第 2 章预告，第一章到第二章不再只是文字按钮跳转
- 教学进度 100% 后刷新会回到教学完成庆祝入口，不再落回项目地图让用户误以为还要重做
- 成长档案已记录 `clearedChapterIds` 和 `unlockedCompanionNames`，复习已通关章节不会重复刷能力分
- 剧情地点已出现本地角色图资产：档案馆记录员、回廊书记官、身份回廊守卫、审判庭书记员、幂等石灵、雾灯猫、模型熔炉执钥人、密钥匣、镜厅校对师、知识馆守卷人、检索狐、塔楼副官、验收试炼官、委托书锻造师、交付审查官、上线守门人、终章答辩官
- 实战 Lab 读取项目材料页已统一为暗色档案馆 RPG，并增加代码阅读导览
- 实战 Lab 顶部流程图已增加“当前这一棒”路线罗盘，会随当前阶段高亮谁把什么交给谁，帮助用户从剧情过渡到真实练习
- 实战 Lab 流程图下已增加“实战接力板”：固定展示上一棒交来、当前要证明、交给下一棒和本题证据路线，专门解决用户在实战中忘记谁把什么交给谁的问题
- 实战 Lab 每个作答题已增加“新手先读卡”：先用人话说明这题到底在问什么、先看哪里、不要怎么写，再让用户进入材料和输入框
- 实战 Lab 项目材料阅读器已增加“先看这几行”和完整材料备查提示，完整代码/日志仍可查看，但第一遍阅读目标是关键行而不是硬啃全文
- 实战 Lab 所有答题卡已增加“我看到 / 它说明 / 下一步”作答支架，帮助新手把证据写成可复述的排查表达
- 实战 Lab 作答支架已增加“填入骨架”按钮，只插入三段标题，不替用户生成答案，降低空输入框阻力
- 实战 Lab 答题卡已增加“提交前检查”，实时提示是否写出证据、证据含义和下一步验证动作
- 实战 Lab 提交前检查已增加“表达完整度 x/3”和“可以保存并继续”完成提示
- 实战 Lab 表达完整度未满时会显示“还差：...”缺口提示，告诉用户下一句该补什么
- 实战 Lab 的“给 Agent 写任务”步骤会切换为“背景 / 边界 / 验收”委托支架，并可填入 Agent 任务骨架，避免和普通排查题混用表达模板
- 实战提交后的成长档案已改为暗色结案舞台，会沿用当前章节背景、HUD、证据三格和下一步清单，不再跳到独立白色结果页
- 实战通关后的成长档案已增加“迁移口令”：工作复盘、Agent 委托、面试讲法三格，把结案证据直接转成可迁移表达
- 服务端实战提交已改为按场景读取必填作答步骤和 `skillIds`；第 2-15 章不再复用第 1 章硬编码步骤，测试报告未通过时仍会阻止生成成长档案
- 本地备份导入已从增量 upsert 改为覆盖恢复；导入备份会先清空学习记录表再重建，避免临时验收步骤、验证事件或能力证据残留
- 本地备份库前端文案已对齐覆盖恢复语义：恢复方式、恢复说明和成功提示都明确“覆盖当前本地学习记录”，不再写“合并恢复”
- 本地备份库覆盖恢复已增加显式确认门禁：用户必须勾选“我知道恢复会覆盖当前本地学习记录”后，恢复按钮才可用，降低误覆盖风险
- 当前委托房间已改成动态主线面板：会根据成长档案自动显示下一章主线、为什么要去、谁把什么交给谁、通关产出和解锁奖励；通关第 1 章后首页会指向第 2 章
- 首页顶部路线状态已从固定“01 当前章节”改成动态“下一章”编号；通关第 1 章后 HUD 会显示 02 下一章，避免与当前委托主线冲突
- 实战读取测试报告后会随 attempt 返回最近一次验证报告；失败时前端显示“测试报告译文”，列出红灯测试和失败原因，避免新手只看到“失败”但不知道查哪里
- 实战测试红灯已升级为导师式巡查提示：每个失败测试会翻译成“流程断点 / 先查材料 / 下一步”，把测试名和失败原因连回本关材料导览、代码位置和修复动作
- 任务简报左侧已改成“当前委托 / 伙伴图鉴 / 面试复盘 / 本地备份”四个房间，默认只展示第一章目标；伙伴背包、复盘册和备份库需玩家主动打开，减少首屏信息过载
- 当前委托房间已增加“成长契约”，展示当前身份、下一阶位、收集进度和下一位可解锁伙伴，让用户打开后知道自己在扮演谁、要成长到哪里
- 成长契约、伙伴卡和章节奖励舞台已补轻量入场、光纹和主行动反馈；`prefers-reduced-motion` 会关闭这些动效，后续不要把它改回静态白色后台卡片
- 全局词库已从白色弹窗改为暗色 RPG 随身词典，并补充 `dialog` 语义；用户在教学桥里可随时打开术语小抄，不再出现白色断层
- 教学桥后续陪练与结算控件已补齐暗色 RPG 壳层；陪练清单、进度胶囊、重置按钮和通关庆祝背景不再露出白色课程页风格
- 教学桥记忆线、结案导师、Agent 委托、结算前夜和陪练控件的默认基础样式已改为暗色 RPG，后续复用这些组件时不应再先写白底再靠外层覆盖
- 代码导读页的逐行翻译已增强为上下文解释：会结合当前输入、输出、流程位置和常见语法/证据模式，说明每一行接了什么材料、交出什么结果、下一步要看什么证据
- 首页当前委托、教学桥正文和实战 Lab 已统一增加“冒险日志”，固定展示已收录、当前任务和下一步，后续转场不要再只靠散落标题提示用户所在位置
- 代码导读页已增加“读码交接单”，每一行拆成收到、动作、交出、证明四格，特别说明 `response.ok` 只能证明接口回信成功，不能证明数据库已保存
- 实战 Lab 已新增“案件路线牌”，从本关配置自动生成事故来源、完整传递路线、第一眼证据材料和最终交付产出，帮助用户进入每章时先理解这关到底在追哪条工作链路
- 教学桥旧基础组件已补暗色兜底覆盖：流程详情、预测题、补课面板、观察引用和庆祝提示在 `.teaching-bridge` 内不应再露出白色课程卡片
- 教学桥已新增“能力护照”，在记忆线和当前步骤之间持续展示正在训练的工作能力、真实工作用法、证据工具和面试产出，避免剧情推进时学习目的断掉
- 能力护照已补“Agent 协作”卡，持续提示背景、边界、验收三件套，让用户从教学阶段就把排查能力连到 Agent 委托能力
- 能力护照的 Agent 协作卡已升级为“Agent 委托骨架”，会按当前站点和证据工具自动给出背景、边界、验收三句示例，后续不要退回空泛提示
- 能力护照已补“验收动作”卡，持续提醒用户要用证据证明当前路线成立，避免只看完剧情或概念就误以为已经掌握
- 能力护照的面试产出卡已新增“面试复盘骨架”，按现象、证据、行动、验证四段自动给出可复述结构，帮助每章产出转成面试材料
- 能力护照的 Agent 委托骨架和面试复盘骨架已支持一键复制；后续不要把它退回只能看的提示卡
- 教学桥已新增“本章流程回放”，会从 `projectMap.nodes/edges` 自动生成完整传递路线，高亮当前站点、证据来源和下一棒，后续不要再手写另一份流程以免和项目地图脱节
- 教学桥已新增“本章导师同行”，直接复用当前章节 `storyScenes` 的角色图、地点、导师台词和第一条线索；后续章节如果补剧情角色，只要补 `storyScenes` 就会同步到教学页
- 教学桥已新增“导师试炼三印章”，让用户点亮流程、证据、委托三项自检；这是轻量 UI 状态，不写入 SQLite 学习记录
- 导师试炼三印章点满后会显示“试炼完成”回响、下一步和本步可带走的一句话，后续不要把它退回纯按钮状态
- 试炼完成回响已新增“复制本步收获”，把当前步骤、流程、证据、Agent 委托和下一步整理成剪贴板文本
- 试炼完成回响已新增“面试里可以说”一句话，并写入复制文本，帮助每个教学步骤都沉淀成求职表达
- 试炼完成回响已新增工作能力、Agent 委托、面试素材三格，用来强化点满印章后的可扫读收获
- 试炼完成回响已新增“获得能力印记”，把当前步骤转成即时奖励并写入复制文本；这是轻量 UI 奖励，不写入持久化档案
- 试炼完成回响和能力印记已新增轻量解锁动效，并在 `prefers-reduced-motion` 下关闭动画
- 导师试炼未点满时会显示还差的印章，点满后收起；这是轻量 UI 引导，不写入 SQLite 学习记录
- 15 章路线 manifest 已增加 `flowPayloads`，章节卷宗会展示每一棒真正传递的材料；第一章是“点击保存和草稿 → POST JSON → 校验对象 → INSERT → 数据库记录”，不要退回用系统名称冒充交接物
- 第一章档案馆记录员已统一使用 `src/assets/portrait-archive-keeper-v2.webp` 高质量男性导师立绘；旧 SVG 暂时保留作回退参照，App 和 TeachingBridge 不再引用它；小尺寸角色框使用脸部优先裁切
- 第 2–4 章已接入同画风新立绘：传送门书记官、身份回廊守卫为男性导师，接口审判庭书记员为女性导师；角色阵容后续保持帅哥美女混合，不要退回同质化男性占位图
- 第 2 章宠物“灵感萤火”使用 `src/assets/pet-inspiration-glow-v2.png`，不再错误复用人物图；宠物审美基线是奶油/杏色、圆润柔软、暖光和亲近感，禁止诡异眼睛、牙齿和脏暗配色
- R143 已通过 `npm run verify`（6 个测试文件、64 个测试）、第 2–4 章桌面/390px 卷宗截图和灵感萤火桌面/390px 图鉴截图；浏览器控制台 0 error/0 warning，390px 无横向溢出
- R144 已完成单屏节奏专项：伙伴/面试/备份房间不再同时渲染右侧路线图；章节卷宗主舞台后只保留“流程证据/工作锦囊”两条默认关闭抽屉；伙伴页 390px 用收藏选择器替代 15 项长列表；教学桥在导师之后立即显示当前任务，记忆线、日志、流程、试炼和能力护照放进默认关闭的“冒险辅助”
- R144 已通过 `npm run verify`（6 个测试文件、64 个测试）及 1200×760/390×844 浏览器截图；两端无横向溢出，浏览器控制台 0 error/0 warning
- 第 5–9 章已接入五张 v2 WebP：`pet-idempotency-stone-v2.webp`、`pet-foglamp-cat-v2.webp`、`portrait-model-warden-v2.webp`、`portrait-mirror-editor-v2.webp`、`pet-retrieval-fox-v2.webp`；三只宠物遵循暖色、圆润、友善基线，第 7/8 章为一男一女导师
- 五张原始 1024×1536 生成图已压缩为约 131–179KB/张再接入，避免新增约 11MB PNG；对应旧 SVG 暂留作合并参照，但 App 与 TeachingBridge 不再引用
- R145 已通过 `npm run verify`（6 个测试文件、64 个测试）、第 5–9 章桌面卷宗与检索狐 390px 图鉴验收；第 7 章入口额外二次截图确认完整，浏览器控制台 0 error/0 warning，390px 无横向溢出
- 第 10–12 章已接入 `portrait-tool-warden-v2.webp`、`portrait-test-arbiter-v2.webp`、`portrait-brief-forgemaster-v2.webp`，角色阵容为两位男性导师和一位女性导师；工具边界、测试证据、任务锻造三种场景与服装可区分
- 第一、二至四章和灵感萤火的 5 张 1.3–1.6MB PNG 已转为 71–134KB WebP 并更新全部引用；当前生成角色/宠物均使用 WebP，目标旧 PNG/SVG 不再被 App 与 TeachingBridge 引用
- R146 已通过 `npm run verify`（6 个测试文件、64 个测试）、第 10–12 章桌面卷宗和第 12 章 390px 截图；控制台 0 error/0 warning，390px 无横向溢出。构建中角色/宠物为 71–203KB/张，剩余主要图片债务是 2.3–2.6MB 场景背景
- R147 已升级第 13–15 章交付审查官、上线守门人和终章答辩官，三位导师均使用压缩 WebP；五张主场景背景由约 11.7MB PNG 压缩为约 1.0MB WebP，旧 PNG 已移除，后续不要恢复旧资源路径
- 宠物视觉以奶油杏、暖金、柔和绿色等亲近配色为基线，造型要圆润、可爱、像可靠伙伴；禁止尖牙、诡异眼神、脏暗颜色和恐怖化表达，世界观的神秘感由场景、光影与剧情承担
- R147 已通过工程运行时 Node 24 的 `npm run verify`（6 个测试文件、64 个测试）以及第 13–15 章桌面、第 15 章 390px 浏览器截图；移动端 `scrollWidth = clientWidth = 390`，控制台 0 error/0 warning
- R148 已把 15 章解锁物接入教学步骤的“同行伙伴反应”：地图提示先念材料接力，术语提示先看类比，代码提示只盯高亮行，证据提示找互证，结案提示整理现象/证据/行动/验证；宠物不再只存在于图鉴和奖励页
- R148 已通过 Node 24 `npm run verify`（6 个测试文件、64 个测试）和第 2 章灵感萤火教学桥桌面/390px 浏览器验收；390px `scrollWidth = clientWidth = 390`，控制台 0 error/0 warning。验收前导出本地学习备份，临时重置第 2 章教学后已覆盖恢复原记录
- R149 已把教学步骤的立即跳转改为“伙伴证据收录”过场：显示完成步骤目标、本章能力印记、下一步骤及其目标；用户点击“收下证据”后才进入下一站，最后一步确认后才进入章节结算，继续按钮自动获得焦点
- R149 已通过 Node 24 全量门禁（6 个测试文件、65 个测试、生产构建）和灵感萤火过场桌面/390px 浏览器验收；390px `scrollWidth = clientWidth = 390`，控制台 0 error/0 warning。浏览器验收完成后已覆盖恢复原学习备份
- R150 已在流程地图和章节结案两个关键过场加入主动复述：地图问“起点把什么交给谁、最后交出什么”，结案问“看什么证据才能证明完成”；至少 12 个字后才能继续，原文以 `teachingResponse.activeRecall` 保存到本地 SQLite，但不评分、不直接改变能力等级
- R150 已通过 Node 24 全量门禁（6 个测试文件、65 个测试、生产构建）、前端 PATCH 原文测试、服务端备份恢复测试和桌面/390px 浏览器验收；390px 无横向溢出，控制台 0 error/0 warning，临时验收记录完成后已恢复原学习备份
- R151 新增只读 `/api/learning-recalls`，只返回章节、步骤、主动复述原文和更新时间；面试复盘房间、作品集预览和 Markdown 会把“我的原始复述”与系统路线参考分开展示，不把用户原话自动改写成标准答案
- R151 的手机端作品集不再先展示 15 个章节按钮，改用章节选择器并把当前章节内容前置；桌面仍保留可扫描的章节列表。桌面与 390px 已验收，移动端 `scrollWidth = clientWidth = 390`，控制台 0 error/0 warning，临时注入的验收记录已覆盖恢复
- R152 正在修正成长状态流：教学完成只显示“伙伴会合 / 等待实战”，不再调用 `awardXP` 或 `completeChapter`；只有 `/api/attempts/:id/submit` 成功返回后，前端才发 XP、记录章节通关并让伙伴正式归队。重复复习沿用已有去重规则，不重复刷分
- R152 保留历史本地成长档案，不自动回滚用户之前已获得的 XP/收藏；这是为了避免无证据覆盖用户数据。后续如需校准历史记录，应基于 SQLite 中已提交 attempt 做显式、可预览的迁移，不得静默清空
- R152 已把教学末站的“进入章节结算”统一改成“前往伙伴会合”，会合页在桌面收成单屏简报，在 390px 使用横向紧凑伙伴卡和三行通关门槛；主行动无需滚过长面试卡即可看到
- R152 已通过 Node 24 `npm run verify`（6 个测试文件、66 个测试、生产构建）和第 2 章伙伴会合桌面/390px 浏览器验收；390px `scrollWidth = clientWidth = 390`，页面仅 64px 纵向余量且主按钮在首屏，控制台 0 error/0 warning
- R153 新增 `scene-identity-corridor.webp`（162KB）、`scene-api-error-court.webp`（122KB）和 `scene-idempotency-forge.webp`（201KB），均为 1920×1081、无文字/人物的章节背景；不要恢复成第 3/4 章共用 `questPortal`、第 5 章复用 `questWorkbench`
- R153 已在 `src/App.tsx` 的 Lab 与章节卷宗映射、`src/TeachingBridge.tsx` 的章节入口和开场/结案剧情地点接入对应背景；章节中间地点仍使用 Cookie 传送门、日志档案库、唯一约束档案库等不同画面，避免整章只换一张静态底图
- R153 已通过 Node 24 全量门禁（6 个测试文件、66 个测试、生产构建）、第 3/4/5 章桌面和第 5 章 390px 浏览器验收；三张图均加载，390px `scrollWidth = clientWidth = 390`，控制台 0 error/0 warning
- R154 新增 `scene-performance-observatory.webp`（249KB）、`scene-model-key-forge.webp`（197KB）、`scene-hallucination-mirror.webp`（216KB）和 `scene-rag-knowledge-maze.webp`（288KB），均为 1920px WebP；不要恢复第 6–9 章原先复用的 `questStage` / `questPortal` / `questArchive`
- R154 已把四张背景接入第 6–9 章入口、开场/结案剧情、卷宗/会合和实战；瀑布钟塔、密钥后台、引用审判席、切页工坊等中间地点继续换景。第 7 章装备会合使用模型熔炉执钥人立绘，实战通过后才交付“密钥匣”
- R154 已通过 Node 24 全量门禁（6 个测试文件、66 个测试、生产构建）、第 6/7/8/9 章桌面和第 9 章 390px 浏览器验收；390px `scrollWidth = clientWidth = 390`，控制台 0 error/0 warning
- R155 新增 `scene-agent-tool-contract-hall.webp`（217KB）、`scene-verification-trial-arena.webp`（163KB）和 `scene-agent-brief-forge.webp`（193KB），均为 1920px WebP；不要恢复第 10–12 章原先复用的 `questPortal` / `questStage` / `questWorkbench`
- R155 已将三张背景接入第 10–12 章入口、开场/结案剧情、卷宗/会合和实战；参数契约厅、权限门禁、单元符文室、集成竞技场、目标铁砧和约束符文台等中段地点继续换景
- R155 已通过 Node 24 全量门禁（6 个测试文件、66 个测试、生产构建）、第 10/11/12 章桌面和第 12 章 390px 浏览器验收；390px `scrollWidth = clientWidth = 390`，控制台 0 error/0 warning
- R156 新增 `scene-delivery-review-court.webp`（211KB）、`scene-release-readiness-gate.webp`（160KB）和 `scene-interview-defense-hall.webp`（208KB），并接入第 13–15 章卷宗、剧情首尾、伙伴会合与实战；中段证物室、钥匙库、备份库、监控塔、STAR 星盘和追问镜仍保留换景
- R156 修复结算前夜缺少 `.teaching-bridge` 外壳的问题；暗色 RPG 样式和本章最后地点背景现在会真实生效，不要移除 `celebration-stage-shell` 外层或把 `--celebration-bg` 改回静态黑底
- R156 通过全量门禁（6 个测试文件、66 个测试、生产构建）、第 13/14/15 章桌面与第 15 章 390px 浏览器验收；390px `scrollWidth = clientWidth = 390`，控制台 0 error/0 warning。`eslint.config.js` 和 `vite.config.ts` 已排除/限定检查范围，避免 `.xds/worktrees/` 污染当前分支证据
- R157 新增 `src/careerRoadmap.test.ts`，逐章验证 15 章顺序、九件套字段、流程交接材料、奖励和 AI 路线事实源；当前全量门禁为 6 个测试文件、80 条测试
- R157 已校准 `src/teaching.ts`、`src/TeachingBridge.tsx` 和 `docs/debt.md` 的完成状态：15 章专属 Lab、作品集和本地备份均已实现；真正未完成的是真人学习效果、延迟变式复测和 Java/前端具体路线。后续不要恢复“第 13 章 Lab 未接”“作品集导出未完成”“备份恢复未实现”等旧文案
- R158 将 `TeachingBridge` 从首页同步依赖改为 `React.lazy` 动态导入，并增加暗色 `Suspense` 章节加载画面；生产主 JS 从 827.04kB 降到 592.34kB（减少约 28.4%），教学桥成为独立 236.68kB chunk
- R158 使用 Node 24 通过全量门禁（6 个测试文件、80 个测试、生产构建）；独立浏览器会话确认首页请求中没有 `TeachingBridge.tsx`，刷新进入 `#chapter-13` 后才请求该模块，章节结算页正常且控制台 0 error
- R159 将原先只存在于代码导读的补课入口接到第一章四张概念卡；用户可以选择术语、语法、项目位置或因果卡点，切换补课方向后回到原概念，补课行为继续写入服务端但不扣分
- R159 把带 `**` 和代码围栏的原始字符串改为 `RemediationLesson` 结构化模型；补课使用墨绿暗色专注弹层，桌面完整落在一屏，390px `scrollWidth = clientWidth = 390`、底部操作可见、控制台 0 error。全量门禁为 6 个测试文件、81 个测试
- R160 为第 2–5 章补齐各章专属学习急救站：分别围绕 Project Brief、Token/Cookie/Session、Payload/400/500/requestId、Idempotency-Key/唯一约束/事务解释术语、代码动作、项目位置和故障因果，不再复用第一章词汇
- R160 将新增内容集中在 `src/remediation.ts`，仅由懒加载的 `TeachingBridge` 引用；不要把大段补课内容移回 `src/teaching.ts`，否则会重新进入首页主包。生产主 JS 保持 593.69kB，教学桥独立 chunk 为 254.56kB
- R160 使用 Node 24 通过全量门禁（7 个测试文件、90 个测试、生产构建）；第 2/4 章桌面和第 5 章 390px 浏览器验收通过，手机端 `scrollWidth = clientWidth = 390`、弹层操作完整可见、控制台 0 error
- R161 为第 6–9 章补齐 AI 核心学习急救站：性能章追资源/TTFB/渲染/缓存时间线，AI API 章追浏览器/服务端密钥/上游 stream，幻觉章追 context/citations/展示前校验，RAG 章追文档/chunk/索引/matches/回答证据
- R161 延续 `src/remediation.ts` 的懒加载所有权边界；生产首页主 JS 保持 593.69kB，教学桥独立 chunk 增至 271.62kB。`src/teachingRemediation.test.ts` 当前守住第 2–9 章所有概念与代码步骤的四类补课、章内事实词和跨章文案独立性
- R161 使用 Node 24 通过全量门禁（7 个测试文件、98 个测试、生产构建）；第 6/8 章桌面和第 9 章 390px 浏览器验收通过，手机端 `scrollWidth = clientWidth = 390`、全部操作在首屏内、三处控制台 0 error
- R161 浏览器验收前导出完整学习备份，临时重置第 6/8/9 章并只种入 map 完成记录，验收后通过 `/api/learning-backup/import` 恢复；恢复结果为 15 个 attempts、94 条 teaching_progress，第 6/8/9 章各 6 条，不要把临时验收进度当作用户真实进度保留
- R162 为第 10–12 章补齐 Agent 协作学习急救站：工具章追注册表/schema/权限/执行与审计，测试章追复现/单测/集成/报告/浏览器证据，委托章追背景/目标/约束/验收/风险与交接
- R162 延续 `src/remediation.ts` 的懒加载所有权边界；生产首页主 JS 保持 593.69kB，教学桥独立 chunk 增至 284.83kB。`src/teachingRemediation.test.ts` 当前守住第 2–12 章所有概念与代码步骤的四类补课、章内事实词和跨章文案独立性
- R162 使用 Node 24 通过全量门禁（7 个测试文件、104 个测试、生产构建）；第 10/11 章桌面和第 12 章 390px 浏览器验收通过，手机端 `scrollWidth = clientWidth = 390`、全部操作在首屏内、三处控制台 0 error
- R162 浏览器验收同样先导出学习备份，临时种入三章 map 进度，验收后完整恢复为 15 个 attempts、94 条 teaching_progress，第 10/11/12 章各 6 条；后续章节验收继续遵守先备份后恢复的规则
- R163 为第 13–15 章补齐职业闭环学习急救站：交付审查章追说明/Diff/测试/边界/文档互证，上线章追计划/生产配置/备份/监控/回滚，面试章追 STAR/故障复盘/技术取舍/成长证据/追问
- R163 延续 `src/remediation.ts` 的懒加载所有权边界；生产首页主 JS 保持 593.69kB，教学桥独立 chunk 增至 298.02kB。`src/teachingRemediation.test.ts` 当前守住第 2–15 章所有概念与代码步骤的四类补课、章内事实词和跨章文案独立性
- R163 使用 Node 24 通过全量门禁（7 个测试文件、110 个测试、生产构建）；第 13/14 章桌面和第 15 章 390px 浏览器验收通过，手机端 `scrollWidth = clientWidth = 390`、底部两个操作均在首屏内、三处控制台 0 error
- R163 浏览器验收前导出完整学习备份，临时重置三章并只种入 map 进度，验收后覆盖恢复为 15 个 attempts、94 条 teaching_progress，第 13/14/15 章各 6 条；截图位于 `output/playwright/r163-chapter13-remediation-desktop.png`、`r163-chapter14-remediation-desktop.png` 和 `r163-chapter15-remediation-mobile-390.png`
- R164 为第一章增加 24 小时延迟变式复测：原画布持久化实战提交后等待 24 小时，改用头像上传后刷新回旧图的新业务、新文件和新证据，重新完成独立诊断、证据计划、Agent 委托和验证反思；有提示时不会生成 L3 候选
- R164 将 `TransferRetestLab` 独立懒加载；TeachingBridge 仍保持动态导入。启动时的复测状态请求为非阻断增强，旧本地 API 暂时不支持新接口时不会拖垮首页
- R164 使用 Node 24 通过全量门禁（7 个测试文件、113 个测试、生产构建）；主 JS 596.32kB、TeachingBridge 298.02kB、TransferRetestLab 10.77kB。教学完成页桌面和 390×844 浏览器验收通过，手机端 `scrollWidth = clientWidth = 390`、主操作按钮底部约 798px、控制台 0 error；截图位于 `output/playwright/teaching-bridge-desktop.png` 和 `teaching-bridge-mobile-390.png`
- R164 复测工作台的桌面/390px 验收同样为零横向溢出、零控制台错误；截图位于 `output/playwright/r164-transfer-retest-desktop.png` 和 `r164-transfer-retest-mobile-390.png`。验收生成的临时复测 attempt 已用 `/tmp/code-quest-r163-restored.json` 覆盖恢复，最终仍为 15 个 attempts、94 条 teaching_progress、0 个复测 attempts
- R165 修复十五章通关后的终局状态：HUD 显示 `15/15 主线完成`，当前委托切换为终章答辩官授勋房间，直接提供面试复盘、面试作品集和第一章延迟复测出口，不再出现“下一章主线：第 15 章”
- R165 世界地图、任务卡和卷宗状态改为从 `clearedChapterIds` 推导“已通关 / 当前 / 待练习”；`loadDeveloper` 与备份恢复会按已通关章节补回后期新增的伙伴，解决十五章通关但收藏仍为 9/15 的旧档案不一致
- R165 使用 Node 24 通过全量门禁（7 个测试文件、115 个测试、生产构建）；主 JS 600.46kB、TeachingBridge 298.02kB、TransferRetestLab 10.77kB。桌面和 390×844 终局页面 `scrollWidth = clientWidth`、主行动在首屏内、控制台 0 error；截图位于 `output/playwright/r165-ai-route-graduation-desktop.png` 和 `r165-ai-route-graduation-mobile-390.png`
- R166 将首次进入和复访入口分开：只有 `missionsCleared === 0` 的新用户播放“代码城失忆夜 → 证据选择 → 领取委托”；已有进度的用户直接由当前章节导师接引，十五章通关用户由终章答辩官打开职业档案。不要恢复每次刷新都重播序章的旧逻辑
- R166 使用 Node 24 通过全量门禁（7 个测试文件、115 个测试、生产构建）；主 JS 602.06kB、TeachingBridge 298.02kB、TransferRetestLab 10.77kB。1200×760 与 390×844 的归城主行动均在首屏内，`scrollWidth = clientWidth`、控制台 0 error；截图位于 `output/playwright/r166-returning-player-homecoming-desktop.png` 和 `r166-returning-player-homecoming-mobile-390.png`
- R167 新增 `sharedCoreAbilities` 事实源，把读项目、追数据流、定位故障、读取证据、委托 Agent、验收交付和讲清项目映射到真实 AI 章节，并分别写清迁移到 AI、Java 后端和前端工程时的工作用途；新增岗位路线不得绕过这份共享契约
- R167 将 Java/前端锁定路线收成单屏预告：左侧只显示未来身份、未开放边界、岗位技能和返回 AI 主线，右侧只显示未来地点与三项迁移实例；不再混入第一章比喻卡、伙伴/备份房间或 AI 长任务板。桌面页面高度为 760px、主行动底部约 619px；390×844 主行动底部约 657px，两端无横向溢出且控制台 0 error
- R167 使用 Node 24 通过全量门禁（7 个测试文件、116 个测试、生产构建）；主 JS 606.25kB、TeachingBridge 298.02kB、TransferRetestLab 10.77kB。截图位于 `output/playwright/r167-java-shared-core-desktop.png` 和 `r167-java-shared-core-mobile-390.png`
- R168 为第 2 章增加 24 小时延迟变式复测：新业务是 AI 会议助手从模糊需求到 Brief、候选筛选、取舍理由和刷新恢复，独立沙盒为 `sandbox/meeting-assistant-retest`，初始报告固定为 0 通过 / 2 失败
- R168 将延迟复测集中到 `src/transferRetests.ts` 配置；第一章使用回声廊线性证据图和横向推轨，第二章使用需求工坊分支桌面图和俯视落镜。新增场景必须遵守 `docs/game-camera-and-map-rules.md`，不能只换背景图
- R168 浏览器只检查入口和布局，没有点击第二章复测去污染真实学习记录；当前本地成长档案显示 15/15，但服务端没有可用于第 2 章复测的已提交源 attempt，因此入口正确显示未解锁。手机端无横向溢出，复测卷宗从第二屏开始，后续仍需继续压缩终章首屏
- R168 使用 Node 24 通过全量门禁（8 个测试文件、119 个测试、生产构建）；主 JS 613.45kB、TeachingBridge 298.02kB、TransferRetestLab 8.16kB。职业档案桌面/390px 均为 `scrollWidth = clientWidth`、控制台 0 error，首页未请求 TeachingBridge chunk
- R175 为 `TeachingBridge` 按需加载增加生产构建防回退门禁：Vite manifest 必须证明它是独立 dynamic entry，首页入口不能静态依赖它；`npm run build` 会自动执行 `scripts/verify-lazy-chunks.mjs`
- R175 使用 Node 24 通过全量门禁（9 个测试文件、134 个测试、生产构建）；主 JS 630.23kB、TeachingBridge 310.46kB、TransferRetestLab 8.16kB。独立浏览器会话确认首页没有请求 `TeachingBridge.tsx`，直达 `#chapter-13` 后才请求；1200×760 与 390×844 均无横向溢出、控制台 0 error，手机主操作底部约 800px
- R175 浏览器截图位于 `output/playwright/r175-lazy-home-desktop.png`、`r175-lazy-chapter13-desktop.png` 和 `r175-lazy-chapter13-mobile-390.png`；`output/` 被 Git 忽略，截图只作为本地验收证据，不上传仓库
- R185 对首页到第 1 章的首次认知路径做了真实浏览器审计：领取委托后的页面从约 4824px 信息墙压缩为默认展开当前委托、其余内容按需展开；迁移复测、后续 2–15 章、四个角色比喻均改为折叠资料，不遮挡当前行动入口
- R185 使用 Node 24 通过全量门禁（9 个测试文件、151 项测试、生产构建）；桌面首屏简报收起态 `scrollHeight=2251`、`scrollWidth=1200`，390px 移动端 `scrollWidth=390`、无横向溢出；浏览器截图位于 `output/playwright/audit-brief-layered-desktop.png`、`audit-brief-layered-mobile-390.png` 和 `audit-brief-layered-mobile-viewport.png`
- R186 将 15 章场景数据中已有的角色台词真正渲染进教学页导师同行面板；当前步骤切换时用场景 id 重新挂载人物面板，触发台词气泡入场动画，保留独立背景、角色和证据步骤。未清空本地学习档案，浏览器对已有完成记录只能验证结算页；代码构建与测试通过
- R186 使用 Node 24 通过全量门禁（9 个测试文件、151 项测试、生产构建）；TeachingBridge 独立 chunk 仍由构建门禁确认按需加载
- R187 为地点切换增加轻量入场动效：背景从新场景淡入并轻微收镜，人物立绘从侧下方进入，台词气泡同步出现；`prefers-reduced-motion` 会关闭这三类新增动画。全量门禁通过
- R188 将序章三种开场选择延续到任务简报：选择会显示为“开场倾向”印记，并给出对应的学习提醒；它改变叙事反馈和本关关注点，不绕过相同的证据验收目标
- R189 领取第 1 章时把开场选择写入 `baseline-plan` 委托记录，作为后续复盘的原始上下文；已有本地档案不会被清空，当前浏览器因复用已完成 attempt 未能再次触发写入请求
- R190 将每章 `workBackground` 传入教学桥角色面板，学习顺序现在明确为“剧情现场 → 工作里什么时候会遇到 → 术语/证据 → 关键代码”；全量门禁通过
- R191 从已有 `baseline-plan` attempt 恢复 `openingChoice` 到首页状态，避免刷新后序章倾向丢失；当前浏览器复用的历史 attempt 没有该字段，因此只验证了兼容性路径，未清空记录制造新 attempt
- R192 修正旧档兼容显示：没有真实 `openingChoice` 时不再伪显示“证据优先”印记；全量门禁通过
- R193 静态审计确认 15 章各自拥有独立镜头、地图拓扑、地点/角色台词和工作背景；同时修正旧档无选择时不显示虚假的序章印记。全量门禁通过
- R194 在引导式代码阅读页新增“谁把什么交给谁”主交接卡：把上一棒、当前代码、下一棒和输入/输出用一句人话连起来，避免初学者只看分散的逐行卡片；新增测试覆盖终点流程词。`npm run verify:quick` 通过，9 个测试文件、151 项测试
- R195 在剧情探索页新增“上一幕回声”：进入下一地点时保留上一地点、角色、刚收录的线索和下一步证据方向；首幕显示序章委托，避免用户进入新地点后忘记为什么继续。`npm run verify:quick` 通过，9 个测试文件、151 项测试
- R196 让当前章节伙伴/宠物在每次收集线索后即时回应：用角色图和白话说明把该证据与能力连接起来，不再只在结算页展示伙伴。`npm run verify` 通过，9 个测试文件、151 项测试
- R197 增加本幕伙伴默契计数：默契印记严格跟随已收集线索数量，线索回应显示当前进度，避免用空 XP 代替学习反馈。`npm run verify:quick` 通过，9 个测试文件、151 项测试
- 章节教学已支持 `#chapter-n` 刷新恢复；用户或验收浏览器可以直接回到对应章节教学桥，已有进度时会跳过前置剧情探索并进入第一个未完成步骤
- 实战通关后的成长档案新增“回到路线图，继续冒险”出口；复访已通关章节时不会停在结案页死路，浏览器已验证可返回首页当前委托
- 工程师成长身份（见习开发者→AI 应用学徒→项目修复手→AI 工程新星）
- 词库面板（15个术语，浮动按钮）
- 基线诊断页面已从前端删除，入口自动创建任务记录
- V1 localStorage 小课、单选题成长规则和对应测试已删除
- `npm run verify` 全量质量门禁通过

**未完成：**

- 主线 1-15 已接入完整 UI 实战修复闭环、面试作品集 Markdown 出口和本地备份库；后续重点转向真人学习效果验收和 Java/前端等岗位路线
- 伙伴/宠物/装备解锁已有首页伙伴背包和章节结算反馈；面试复盘册已支持进入复盘房间填写草稿，并支持打开作品集 Markdown 出口
- 真人学习效果验收
- Java 后端和前端工程路线已有 manifest 占位和首页锁定态，但尚未接入具体章节

### R198：序章委托人入场

序章警报现在会显示与档案馆同世界观的档案馆记录员立绘，明确“谁发现了数据消失、谁把委托交给玩家”。桌面端与警报正文同屏，移动端改为小立绘上置；`src/App.test.tsx` 覆盖角色可见性，浏览器已检查桌面与 390px 移动端无横向溢出。`npm run format:check` 与入口测试通过。

### R199：本幕任务契约

剧情探索地点新增“本幕任务契约”，直接显示当前地点、要找的流程材料以及找到后要交给的下一站，并复用真实 journey 数据。完整流程图仍保留用于全局理解；第一章浏览器主流程测试覆盖契约文本，移动端使用单列布局。

### R200：任务契约连接真实工作

任务契约增加“真实工作现场”提示，读取 `TeachingBridge` 的 `workBackground`，让用户在剧情调查开始前知道这不是纯比喻游戏，而是对应保存失败、登录丢失、接口故障等真实工程问题。第一章主流程测试覆盖该文案。

### R201：移动端路线目录前置

移动端路线大厅将 `.intro-panel`（15 章世界地图与任务板）前置到当前委托长说明之前。390px 浏览器实测第一章节点约在首屏 `y=209` 出现，15 个节点均可见且无横向溢出；桌面端不受该媒体查询影响。

### R202：路线节点可探索

世界地图节点现在使用可访问按钮，点击任意 AI 章节会选中对应卷宗并滚动到角色、背景、流程和伙伴说明；锁定章节仍只有预览，不会调用 `startChapter`。第一章主流程测试覆盖点击第 2 章节点后的卷宗切换。

### R203：路线地图操作提示

地图标题下新增“点击章节查看卷宗；只有当前章节可以进入实战”提示，避免可探索节点和可进入关卡被误解为同一动作。

### R204：路线节点选中反馈

地图节点新增独立 `.selected` 视觉状态，和 `active` 当前关卡、`solved` 已通关状态分离；测试覆盖点击第 2 章后节点的 `aria-pressed` 与 class。

### R205：每幕主动复述

剧情地点现在有一个轻量主动复述门槛：线索与本幕复盘完成后，用户需要用自己的话回答“这一幕的证据证明了什么，下一幕要继续查什么”，再点击“封存本幕复述”前往下一地点。原话按 scene id 随本关完成响应提交，UI 明确说明不自动判定对错，也不把复述本身直接当作能力证明。第一章和第二章测试已覆盖完整复述流程。后续真人试玩需要观察这个门槛是否帮助理解，是否出现机械填写。

### R206：剧情页信息分层

剧情探索页将完整流程改为可展开的“完整流程卷轴”。第一幕默认展开，帮助建立全局地图；进入后续地点时默认收起，只保留上一幕回声、本幕任务契约、当前地点和线索。流程图、节点说明和当前高亮棒位仍然存在，用户可以随时展开查看。这个改动针对“看不懂但信息太多”的认知负担，后续真人试玩重点观察用户是否能在不展开全图时复述当前任务。

### R207：每幕判断分支

线索收齐后新增“做出判断”环节。玩家可以选择先收下眼前结果，也可以沿证据继续追到下一站；两种选择都不会扣分，但会展示不同的解释反馈。选择通过 `aria-pressed` 表达，并与本幕复述一起保存到故事关卡的完成响应。后续真人试玩要重点观察：玩家是否理解两种选择的证据边界，还是只机械点击推荐项。

### R208：地点 NPC 阵容

新增 `scenePortraitOverrides`，按剧情地点覆盖人物头像。它不改变教学数据和流程，只让场景角色真正随着地点变化：性能章节会出现时间导航员和回声调查官，接口/日志章节会出现索引仲裁者，RAG/Agent 章节会出现检索伙伴与权限调查角色。当前地点卡、导师同行和下一地点预告共用 `getScenePortrait`，确保人物、地点和台词保持一致。真人试玩仍需观察角色变化是否帮助记住证据棒位，而不是增加视觉噪音。

### R209：伙伴图鉴成长预告

伙伴图鉴现在区分“已归队”和“等待解锁”：未收集项使用剪影与“完成本章后揭晓”，详情只显示下一步学习方向；已收集项才展示真实角色、能力印记、工作场景和面试复盘。图鉴顶部会显示下一位未收集伙伴的章节预告。筛选器同时修正了详情跟随筛选结果的问题。后续真人试玩重点观察：预告是否激励继续学习，剪影是否足够可理解，以及用户是否仍然误以为收藏状态等于能力掌握。

### R214：剧情进度可恢复

故事探索现在会把当前地点、已收录线索、本幕复述和判断选择保存为未完成教学记录。刷新或重新进入同一个教学桥时，会读取这份快照并恢复到对应地点；已收录线索仍可回看，不会重复计数。故事完成时复用同一个步骤 id 写入 `completed: true`，后续教学不会被未完成快照拦截。

本次新增了故事步骤 id 映射和响应字段过滤，恢复时会限制地点索引并忽略非法字段。验证应覆盖：找到线索后刷新、恢复当前地点、回看已收录线索、完成故事后进入后续步骤。当前自动化质量门禁已通过，真实用户连续学习和弱网络保存体验仍待观察。

### R215：根地址继续未完成委托

应用从根地址启动时，现在会检查第一章当前尝试的教学记录。只要用户已经开始过剧情或完成过教学步骤，就会直接回到教学桥；没有记录的新用户仍从序幕开始，全部完成的用户仍进入完成状态。这个入口层恢复和 R214 的剧情快照恢复配合，解决“刷新后回到首页，看起来像学习丢了”的认知断点。已增加 App 回归测试，真人还需验证用户是否能一眼看懂“正在继续哪份委托”。

### R216：岗位路线 manifest 实体化

`src/careerRoadmap.ts` 现在包含 `javaBackendRoadmap` 和 `frontendEngineeringRoadmap`，每条路线各有 4 个完整章节契约。它们仍标记为“即将解锁”，因为真实沙盒、教学桥场景和服务端材料白名单尚未接入；这是一种有意的边界，避免用户点击后进入没有证据任务的空关卡。岗位选择页会展示真实的章节数量和预告，AI 主线仍是默认可玩路线。

### R217：Java 第 1 关沙盒材料落地

已新增 `sandbox/java-layered-request/` 和 `java-layered-request` 服务端场景注册。沙盒事故是 `UserController` 直接调用 `UserRepository`，绕过 `UserService` 的 viewer 权限规则；`npm test` 初始结果为 1 passed、2 failed，学习者需要修复 Controller 后重新测试。`src/teaching.ts` 已有 Java 项目地图和名词/代码导读数据。

当前 Java 第 1 关已开放到教学桥：App 路由、Java 专属剧情背景、五站证据路线、伙伴和浏览器入口均已接入。R218 的验收重点是从首页职业档案选择 Java 后，确认教学桥显示“请求为什么要经过三层”，并检查刷新恢复仍停留在 Java 委托。Java 沙盒修复后的真实测试报告闭环尚未完成；前端路线仍未接入真实沙盒和教学桥。

### R218：Java 第 1 关接入教学桥

Java 入口现在从职业档案的 Java 后端路线进入 `java-layered-request`。教学桥有五站独立剧情、流程卷轴、名词解释、Controller/Service/Repository 代码证据、Network/日志/数据库材料和面试复述。此前出现的“点击 Java 却看到 AI 第一关”已定位为教学桥未知场景默认回退问题，已改为 Java 专属场景分支。

浏览器验收路径：打开根地址 → 打开职业档案 → Java 后端 → 进入第 `java-1` 章教学关卡 → 开始闯关。预期首屏标题为“请求为什么要经过三层”，调查路线为“请求城门 → Controller 接待厅 → Service 规则熔炉 → Repository 档案库 → 面试答辩台”。

### R219：前端第 1 关组件剧场

前端工程路线现在也是“可进入”，第一个真实场景为 `frontend-component-state`。它围绕 `ProfilePanel.jsx` 的状态提前变成 success 展开，教学桥会依次经过组件剧场入口、状态灯控台、Network 回廊和可见反馈舞台，解释组件、state、重新渲染、`response.ok` 和可访问错误反馈。

浏览器验收路径：打开根地址 → 打开职业档案 → 前端工程 → 进入第 `frontend-1` 章教学关卡 → 开始闯关。预期首屏标题为“按钮为什么一点击就乱跳”，调查路线为“组件剧场入口 → 状态灯控台 → Network 回廊 → 可见反馈舞台”。沙盒初始测试预期 `2 passed, 2 failed`，红灯是缺少 loading 和失败不可见；应用不执行命令，只读取用户生成的报告。

### R220：岗位路线成长档案归档

`src/careerProfile.ts` 原先只把数字章节 `1-15` 当作有效成长记录。现在会从三条职业路线的 manifest 收集全部章节 ID，因此 `java-1`、`frontend-1` 等岗位章节的 XP、通关状态和伙伴也会进入同一成长档案。新增测试覆盖首关/后续关卡奖励和岗位伙伴归档。

### R221：剧情到实战的用户路径复测

浏览器已完整走通前端第 1 关的剧情路径，包含 4 个地点、8 条线索、判断、主动复述、实战前会合和前端 Lab 入口。过程中发现并修复了三个用户问题：底部推进按钮与复述按钮同名、前端教学显示了 AI 工作背景、实战提交按 AI 第 1 章结算。现在推进按钮文案为“继续下一地点”等明确状态，工作背景和结算章节跟随活动岗位路线。

尚未完成的验证：用户在 Lab 中手动修复 `sandbox/frontend-component-state/frontend/ProfilePanel.jsx` 后运行 `npm test`，应用读取通过报告，再完成必填作答和提交结算。这一步不能由应用自动执行命令。

## 强制阅读顺序

1. `AGENTS.md` — 项目规则、安全不变量
2. `docs/cross-computer-handoff.md` — 跨电脑拉取、当前远端提交、完成情况、下一步和合并建议
3. `docs/ai-career-rpg-tasks.md` — 当前 AI 职业 RPG 工程化任务入口
4. `docs/cx-ai-career-rpg-home-merge-notes.md` — 当前分支继续开发、提交和合并注意事项
5. `docs/guided-learning-spec.md` — 引导式教学需求
6. `docs/guided-learning-tasks.md` — 引导式教学历史实施清单
7. `docs/handoff-guided-learning.md` — 用户反馈与产品目标
8. `docs/v2-architecture-proposal.md` — 架构与安全边界

## 技术栈

React 19 + TypeScript 5 + Vite 7 · Node HTTP API · Node SQLite (schema v2) · Vitest

## 关键文件

| 文件                     | 说明                                          |
| ------------------------ | --------------------------------------------- |
| `src/App.tsx`            | 主入口：AI 职业路线→教学桥→实战练习           |
| `src/careerRoadmap.ts`   | AI 应用开发 15 章路线 manifest                |
| `src/TeachingBridge.tsx` | 教学桥组件（地图/概念/代码/补课/示范/陪练）   |
| `src/teaching.ts`        | 第一关到第十五关教学数据                      |
| `src/careerProfile.ts`   | 工程师成长身份/XP/通关章节/解锁物             |
| `server/db.ts`           | schema v2 `teaching_progress` 表              |
| `server/store.ts`        | 教学进度保存/读取/重置，复盘草稿保存到 SQLite |
| `server/app.ts`          | 教学API端点                                   |
| `server/scenarios.ts`    | 场景注册表                                    |
| `src/styles.css`         | 全部样式（教学桥+封面在末尾）                 |

## 标准命令

```bash
npm install
npm run dev        # 启动前后端
npm run verify     # 全量质量门禁（format+lint+type+test+build）
npm run verify:quick  # 快速检查
```

## 安全边界（禁止修改）

- Web/API 不导入 child_process，不执行 shell
- 服务只监听 127.0.0.1
- 沙盒路径固定校验
- localStorage 只存 UI 偏好，能力证据在 SQLite

## 分支规则

- `main` 是冻结审查基线，不在其上开发
- 开发在 `cx/ai-career-rpg-home` 或新的 `cx/` 功能分支
- 不合并、不推送 PR 除非用户明确授权
- Handoff 规则：新 Agent 必须先读本文件

## Git 信息

- 冻结基线：`d21721e510a0c2572c343db09c0f2e3daab9b945`
- 开发分支：`cx/ai-career-rpg-home`
- HEAD 哈希：见 `git rev-parse HEAD`

## R235：岗位路线独立剧情补齐

Java 缓存观测不再复用性能章节的迷雾场景，改为“缓存潮汐码头、缓存标签星图室、旧影镜廊、降级灯塔、命中率观测台”五幕路线；前端无障碍交付不再复用上线章节的城门场景，改为“可见性灯廊、键盘桥、语义档案馆、状态回声庭、无障碍验收台”五幕路线。每幕都有对应角色、对白、名词卡、证据任务和面试产出。

- 验证：Node `v24.13.1` 下 `npm run verify` 通过：格式、Lint、TypeScript、9 个测试文件 / 155 个测试、生产构建和 TeachingBridge 懒加载检查全部通过。浏览器已确认 Java 路线档案显示缓存风廊与缓存巡航员；教学舞台仍需独立空白数据空间验收。
- 当前分支仍未提交、未推送、未合并，等待原 Agent 审查。

## R222：岗位章节数据源修正

`src/App.tsx` 的岗位卷宗现在从当前 `CareerRoute.chapters` 读取，不再在 Java/前端路线中错误回退到 AI 主线章节。教学完成结算、工作背景、流程证据和岗位章节选择都使用当前岗位路线对象；后续扩展 Java/前端第 2 关时，继续沿 `currentMission` 对应岗位章节接入场景与沙盒。

## R223：前端岗位第 2 关已接入

新增 `frontend-request-states`：独立沙盒、Network/浏览器日志/Agent 交付材料、教学步骤、表单传送厅剧情和实战 Lab 配置已接通。进入条件是前端第 1 关完成后，路线会把下一章切换到 `frontend-2`，不再重复进入第 1 关。当前测试：Lint、TypeScript、App/服务端/存储相关 91 项测试通过。

## R224：Java 第 2 关已接入

新增 `java-transaction-consistency`：事务熔炉故事、OrderService/OrderRepository 关键代码、订单/库存证据、独立沙盒和 Lab 配置已经接入。Java 第 1 关完成后，岗位路线会进入 `java-2`，结算使用 Java 路线章节和「事务小锻炉」奖励。

## R225：Java 第 3 关已接入

新增 `java-cache-observability`：缓存风廊剧情、ProjectCacheService.java 关键代码、缓存/数据库/异步时间线材料、独立沙盒和 Java 路线第 3 关入口已经接通。Java 第 2 关完成后会进入 `java-3`，奖励伙伴为「缓存巡航员」。

## R226：前端第 3 关已接入

新增 `frontend-performance-proof`：首屏观测塔剧情、性能证据教学、独立沙盒、性能 Lab 和「首屏观测师」伙伴已经接通。前端第 2 关完成后会进入 `frontend-3`。

## R227：Java 第 4 关已接入

新增 `java-release-harbor`：上线港剧情、ReleaseGate.java 关键代码、配置/备份/健康检查/监控/回滚材料、独立沙盒和 Java 路线第 4 关入口已经接通。Java 路线四关现在均有岗位章节结算和伙伴/宠物奖励。

## R228：前端第 4 关已接入

新增 `frontend-accessibility-proof`：无障碍交付庭剧情、语义 HTML/键盘/焦点/移动端回归材料、独立沙盒和前端路线第 4 关入口已经接通。前端路线四关现在覆盖组件状态、请求状态、性能证据和可交付性审查。

## R229：前端第 5 关已接入

新增 `frontend-testing-proof`：前端回归试炼场剧情、失败复现/自动化/浏览器复测/源码指纹/回归风险材料、独立沙盒和前端路线第 5 关入口已经接通。沙盒初始测试红灯，必须由用户手动修复后再由应用读取固定报告。

## R230：跨岗位图鉴与章节显示修正

伙伴图鉴现在跟随当前职业路线显示，Java/前端路线新增的伙伴不会再被 AI 图鉴遮蔽。岗位路线页面统一显示数字章节，不再暴露 `frontend-1`、`java-1` 这类内部 ID；路线进度也按当前路线单独统计。浏览器验收确认前端路线显示 5 章、包含「回归试炼场」、初始收藏为 `0/5`。

- 验证：`npm run verify:quick`，9 个测试文件、155 个测试通过；浏览器路线切换断言通过。

## R231：Java 第 5 关线上事故闭环

新增 `java-production-incident`：事故回声塔剧情、`IncidentTimeline.java` 关键代码、错误率/P95/结构化日志/止血/回滚/复测证据、独立沙盒和 Java 路线第 5 关入口已经接通。Java 路线现在显示 5 章，TeachingBridge、伙伴图鉴、卷宗背景、结算和工作背景均按新场景接入。

- 验证：Node `v24.13.1` 下 `npm run verify:quick`，9 个测试文件、155 个测试通过；生产构建与浏览器验收待本轮完成。
- 注意：`sandbox/java-production-incident` 初始测试保持红灯，应用只读取固定报告，不会替用户执行命令。

## R232：Java 第 5 关独立场景与真实浏览器验收

`java-production-incident` 不再复用第 4 关上线港的故事配置。TeachingBridge 现在为报警塔、时间线档案、日志回廊、决策门、回滚室和复盘厅提供六个独立地点、六段证据接力和不同角色；第 5 关入口已确认显示「事故回声塔」，不再显示旧的「上线港」。

- 验证：Node `v24.13.1` 下 `npm run verify`，格式检查、Lint、TypeScript、155 个测试和生产构建通过。
- 浏览器验收：入口归城页为统一暗色舞台；岗位档案可切换 Java 路线并显示 5 章；点击第 5 关后显示「报警、止血与回滚复盘」「事故回声塔」及独立角色/背景。
- 当前状态：分支 `cx/ai-career-rpg-home` 仍未提交、未推送，等待原 Agent 审查。

## R233：剧情转场角色登场卡

场景切换不再只显示一行地点文字。下一幕会先以角色立绘、地点、调查目标、对白和“这一幕要接住的证据”出现；转场标题使用预告语义，等动画结束后才进入下一地点，避免把预告误当成已经到达。

- 验证：155 个测试通过；转场回归覆盖 AI 第 2 章和主线第一章的下一地点路径。
- 浏览器语义回归还确认下一幕角色立绘和调查目标会在转场卡中出现，之后才进入新场景。

## R234：本章地点航线

剧情舞台顶部新增固定地点航线，显示已到达、当前调查和下一站角色；移动端改为横向路线条。航线使用短地标名并保留完整地点提示，避免和下一地点预告重复，同时让用户不必靠记忆串起多幕剧情。

- 验证：AI 主线第一章路径回归通过；155 个测试、生产构建和懒加载检查通过。

## R236：新用户序章与独立空白空间验收

在隔离 SQLite 数据库中完成真实浏览器入口验收：新用户从 `Lv.1 · 见习开发者` 进入代码城失忆夜，选择「调取现场证据」后能看到向导解释、领取委托、第 1 章「数据为什么消失」、玩家目标、流程证据入口、工作锦囊和「进入主线」按钮。该路径验证了用户先理解事故和交接关系，再进入教学，而不是直接面对陌生代码。

- 验证：Node `v24.13.1` 下 `npm run verify` 通过；隔离端口 `5174` 与临时数据库完成浏览器验收。
- 当前状态：分支 `cx/ai-career-rpg-home` 仍未提交、未推送；本轮临时验收服务使用独立数据库，不污染用户已有学习记录。

## R237：路线入口与最终门禁复核

复核岗位档案的解锁语义：后续章节可以查看完整卷宗、剧情舞台、流程交接、名词和验收目标；只有当前章节进入实战，避免新手跳过前置证据链。浏览器确认 Java 后端入口可进入第 1 章「请求为什么要经过三层」，并显示 Controller → Service → Repository → 数据库 → HTTP 响应的完整链路。

- 验证：Node `v24.13.1` 下 `npm run verify` 通过：格式、Lint、TypeScript、9 个测试文件 / 155 个测试、生产构建和 TeachingBridge 懒加载检查全部通过。
- 环境备注：系统默认 Node 不支持 `node:sqlite`，本轮已使用项目指定 Node 24 完成验证；这不是代码回归。
- 当前状态：分支 `cx/ai-career-rpg-home` 仍未提交、未推送、未合并；等待原 Agent 审查。

## R238：当前委托入口语义修正

岗位路线页任务板原本使用“进入主线”，用户选中第 3 章卷宗后容易误以为该按钮会进入第 3 章，实际却会启动当前委托第 1 章。现在改为“进入当前委托”；选中章节的进入动作只保留在对应卷宗下，并明确显示“进入第 X 章教学关卡”。

- 验证：Node `v24.13.1` 下 `npm run verify` 通过，9 个测试文件 / 155 个测试、生产构建和懒加载检查通过。
- 当前状态：分支仍未提交、未推送、未合并。

## R239：岗位章节契约防复用门禁

路线测试加强为同时检查 Java/前端每章的九件套数组非空、流程交接数量、世界场景唯一和剧情场景唯一，避免未来新增关卡只替换标题却复用空壳内容。

- 验证：Node `v24.13.1` 下 `npm run verify` 通过，9 个测试文件 / 155 个测试、生产构建和懒加载检查通过。

## R240：路线卷宗的场景与角色去复用

Java 第 5 关改用「信号风暴调度塔」背景，前端第 1/5 关分别使用「记忆回声画廊」和「交付审查厅」，避免岗位路线的卷宗卡回到上一关的旧场景。Java 第 2/3 关和前端第 3/5 关改用专属角色立绘，不再把宠物头像当作主引导角色，也不重复使用同一张审查官立绘；宠物仍保留在伙伴系统中。

- 验证：Node `v24.13.1` 下 `npm run verify` 通过，9 个测试文件 / 155 个测试、生产构建和 TeachingBridge 懒加载检查通过；浏览器确认 Java 后端与前端工程均显示 5 章独立路线与当前委托入口。
- 环境边界：当前 in-app browser 后端不提供强制视口切换能力，本轮未把桌面浏览器结果冒充为全路线 390px 逐屏验收；已有移动端回归证据仍按历史记录保留。
- 当前状态：分支仍未提交、未推送、未合并。

## R241：390px 新用户主流程与教学桥验收

使用临时 API 端口 `4318`、临时 SQLite 和 Vite 端口 `5174` 隔离正式学习记录，真实浏览器以 `390×844` 从序章走到第一章 TeachingBridge。序章、开场选择、岗位档案、Java/前端路线目录、第一章开场和教学舞台均无横向溢出；教学桥按需加载成功，并显示“上一幕回声、本幕任务契约、完整流程卷轴、当前交接、名词小抄、探索线索”。

- 验证：`scrollWidth = clientWidth = 390`；TeachingBridge 资源已加载；截图保存在 `output/playwright/.playwright-cli/`，临时数据库未污染正式记录。
- 当前状态：分支仍未提交、未推送、未合并；真实学习效果仍需用户试玩后判断。

## R242：移动端地点航线改为两列全览

教学桥在 `max-width: 720px` 时不再把本章地点航线做成横向滑轨。四幕路线现在以两列网格全览，当前地点、下一站角色和后续地点同时可见；桌面端仍保持横向接力布局。

- 验证：真实 `390×844` 浏览器截图确认 4 个地点以 `2×2` 显示；`scrollWidth = clientWidth = 390`。
- 当前状态：分支仍未提交、未推送、未合并。

## R243：剧情选择回声接力

场景判断不再只在当前幕显示反馈。玩家选择「沿证据继续追到下一站」或先收下表面结果后，下一幕会出现「上一幕判断回声」，明确说明这次选择留下的调查提醒；选择不会惩罚用户，但会把学习重点带入下一幕。选择记录仍通过 `sceneDecisions` 持久化。

- 验证：9 个测试文件 / 155 个测试、生产构建和懒加载检查通过；真实 `390×844` 浏览器确认选择后进入下一幕显示「你选择继续追证据」及交接说明，且 `scrollWidth = clientWidth = 390`。
- 当前状态：分支仍未提交、未推送、未合并。

## R244：场景判断选项绑定当前线索

每幕判断按钮不再重复使用抽象的“先收下眼前的结果”。现在会把当前幕实际探索到的线索写进选项，例如「先把‘检查绿色灯牌’当作结果」，另一项继续指向下一站；玩家能知道自己正在判断哪一份证据。

- 验证：App 教学路径测试覆盖当前线索绑定的按钮文案；Node `v24.13.1` 下 `npm run verify` 通过，9 个测试文件 / 155 个测试、生产构建和懒加载检查通过。

## R245：第一章角色接力补齐

第一章四幕不再让档案馆记录员的立绘重复占据后半段。档案库深处改用知识馆守卷人，修复台改用委托书锻造师；角色名、地点、对白和背景仍分别保持原有剧情语义。

- 验证：角色路径测试通过；Node `v24.13.1` 下 `npm run verify` 通过，9 个测试文件 / 155 个测试、生产构建和懒加载检查通过。

## R246：伙伴图鉴未解锁轮廓提亮

真实 390px 图鉴截图发现未解锁角色的暗化滤镜过重，卡片几乎看不出人物轮廓。现在保留灰阶和神秘遮蔽，但提高亮度与不透明度，让用户能感知“这里等待一位角色”，同时不提前看清完整立绘。

- 验证：真实 `390×844` 浏览器图鉴截图复核；Node `v24.13.1` 下完整 `npm run verify` 通过，9 个测试文件 / 155 个测试、生产构建和懒加载检查通过。

## R247：序章向导人物接力

序章从档案馆警报进入调查选择时，不再从男性角色立绘退回抽象机器人图标。选择场景继续展示档案馆记录员的立绘和身份称呼，让玩家知道是同一位向导在陪伴自己做第一次判断；移动端同时收紧上方留白和立绘尺寸，三个行动选项在 `390×844` 首屏内完整可见。

- 验证：真实桌面与 `390×844` 浏览器确认向导立绘存在、页面无横向溢出、移动端第三个选择按钮完整可见；Node `v24.13.1` 下完整 `npm run verify` 通过，9 个测试文件 / 155 个测试、生产构建和懒加载检查通过。

## R248：代码材料先看关键行

材料阅读页默认不再把完整源码直接铺给新手。现在根据当前材料导览里的原文或语义关键词提取命中行及前后一行上下文，显示行号和“新手阅读模式”；用户确认这一棒后可主动展开完整卷宗，切换材料时会重新回到关键行模式。

- 验证：App 测试覆盖关键行模式与完整卷宗展开；真实 `390×844` 浏览器确认产品 Brief 默认显示 `projectName`、`direction`、`fetch` 相关上下文，点击后 `aria-expanded=true` 且完整源码出现，页面无横向溢出；Node `v24.13.1` 下完整 `npm run verify` 通过，9 个测试文件 / 155 个测试、生产构建和懒加载检查通过。

## R249：移动端实战任务前置

真实 390px 截图发现实战页先堆叠冒险日志、流程地图、案件路线和接力板，当前要填写的任务被推到首屏下方。现在移动端把当前响应卡、基线卡或测试验收卡前置；冒险日志把“当前任务”置顶，已收录和下一步压成紧凑记忆卡，完整地图仍可向下回看。桌面端信息顺序不变。

- 验证：真实 `390×844` 浏览器确认首屏露出「当前任务 · 读 Project Brief」和题目入口，`scrollWidth = clientWidth = 390`；Node `v24.13.1` 下完整 `npm run verify` 通过，9 个测试文件 / 155 个测试、生产构建和懒加载检查通过。

## R250：实战验收目标前置

验收卡新增「本次验收目标」：先解释本次要证明“修复能被测试复现并通过”，再解释通过后会解锁下一棒并写入成长档案；失败被描述为下一处调查线索，而不是扣分。手动步骤继续明确沙盒目录、禁止修改测试、用户手动运行 `npm test` 和应用只读取报告；按钮改为「读取结果」。

- 验证：真实 `1280×900` 与 `390×844` 浏览器确认目标卡、通过结果说明和「读取结果」按钮存在，两个视口均无横向溢出；Node `v24.13.1` 下完整 `npm run verify` 通过，9 个测试文件 / 155 个测试、生产构建和 TeachingBridge 懒加载检查通过。
- 当前状态：分支 `cx/ai-career-rpg-home` 仍未提交、未推送、未合并；本轮仍需用户真人连续试玩来判断学习理解和可玩性。

## R251：三条岗位路线入口审计与文案收口

真实浏览器逐一检查 AI 应用开发、Java 后端和前端工程入口：AI 显示 15 章，Java/前端各显示 5 章；三条路线均显示自己的当前委托、世界地图、第一关主题、角色立绘和剧情背景，没有回退到 AI 第一关。修正 Java/前端目标句自带句号、界面再次补句号造成的“。。”。

- 验证：浏览器确认 AI「数据断层」、Java「分层服务塔」、前端「组件剧场」三套入口语义正确；Node `v24.13.1` 下完整 `npm run verify` 通过，9 个测试文件 / 155 个测试、生产构建和 TeachingBridge 懒加载检查通过。
- 当前状态：分支 `cx/ai-career-rpg-home` 仍未提交、未推送、未合并；三条路线的真人修复沙盒结算仍需连续人工试玩，不以入口审计代替学习效果验收。

## R252：岗位关卡场景独立契约

实战配置修正为真正的岗位场景接力：前端第 1 关使用记忆回声画廊，第 2 关使用传送厅，第 3 关使用首屏观测塔，第 4 关使用交付审查庭，第 5 关使用回归试炼场；Java 第 5 关使用信号风暴调度塔。新增岗位实战场景契约测试，检查每条路线的 scenarioId、背景唯一性和沙盒路径，避免新增关卡悄悄回退到默认 AI 场景。

- 验证：Node `v24.13.1` 下完整 `npm run verify` 通过，9 个测试文件 / 156 个测试、生产构建和 TeachingBridge 懒加载检查通过；浏览器路线入口仍保持 AI 15 章、Java/前端各 5 章。
- 当前状态：分支 `cx/ai-career-rpg-home` 仍未提交、未推送、未合并；真人沙盒修复、报告读取和结算仍需人工连续试玩。

## R253：通过报告证据回显

实战验收页补齐通过态证据：读取到通过报告后，除“真实测试已通过”外，还显示「全部通过 · N 项证据」、通过测试名称和“这份报告证明”说明；失败态仍显示红灯测试、流程断点、先查材料和下一步。`Attempt.latestVerification.report` 现在也保留可选 `summary` 类型，避免通过结果只剩一个绿色状态。

- 验证：Node `v24.13.1` 下完整 `npm run verify` 通过，9 个测试文件 / 157 个测试、生产构建和 TeachingBridge 懒加载检查通过。
- 当前状态：分支 `cx/ai-career-rpg-home` 仍未提交、未推送、未合并；真实通过报告仍必须由用户在自己的沙盒手动运行测试后读取，应用不会替用户执行命令。

## R254：作答检查从标签变成有内容

修正 `ResponseForm` 的提交前检查：Agent 委托必须在「背景/现象」「范围/约束」「验收标准」后写出实质内容，普通证据题必须有证据、证据含义和下一步验证动作；只点击「填入骨架」不会再显示完整。这个检查只是拦截明显空答案，不把关键词或字数冒充理解。

- 验证：Node `v24.13.1` 下完整 `npm run verify` 通过，9 个测试文件 / 157 个测试、生产构建和 TeachingBridge 懒加载检查通过。
- 当前状态：分支仍未提交、未推送、未合并；真人仍需用自己的话完成答案，系统不会自动判定“已经学会”。

## R255：教学舞台前置本关交付契约

教学导师同行卡新增紧凑的「本关交付契约」：在当前任务旁明确显示“现在要证明”“交给下一棒”和“学完能带走”。它直接读取当前项目地图节点的输入输出与证据来源，避免新手必须先展开“冒险辅助”才能理解这一关和工作的关系；完整流程回放、Agent 委托、验收动作和面试产出仍保留在辅助区，控制首屏信息量。

- 验证：TeachingBridge 测试覆盖交付契约默认可见；待运行 Node `v24.13.1` 下完整 `npm run verify` 与真实浏览器桌面/移动端复核。
- 当前状态：分支仍未提交、未推送、未合并；真人仍需用自己的话完成答案，系统不会自动判定“已经学会”。

## R256：岗位剧情角色与场景拆分

修正两处会让玩家感觉“只是换了标题”的复用：Java 事务关现在使用订单锻造台、幂等门廊、唯一索引审查庭、事务回滚试炼场和订单答辩厅，并由订单值守官、幂等门卫、数据审查官、回滚试炼官和工程答辩官接力；前端性能关改用首屏计时港、浏览器瀑布观测台、接口时钟塔、渲染舞台审查席和性能回归试炼场，并由时序领航员等角色接力。新增 `portraitOverride`，避免旧场景的角色覆盖岗位专属立绘。

- 验证：TeachingBridge 测试覆盖 Java 事务和前端性能的专属地点、角色文案与立绘资源；Node `v24.13.1` 下定向 App 测试 41 条通过。真实浏览器确认 Java 第 2 章“事务熔炉”、前端第 3 章“首屏观测塔”入口与角色文案，390×844 下 `scrollWidth = clientWidth = 390`。
- 当前状态：分支仍未提交、未推送、未合并；真人仍需用自己的话完成答案，系统不会自动判定“已经学会”。

## R257：全路线剧情背景唯一契约

审计发现多个章节在地点切换时复用同一张背景，削弱了探索感。现在 AI 15 章、Java 5 章和前端 5 章统一从 TeachingBridge 剧情注册表读取，所有同章幕的背景图必须唯一；Java 上线关和前端测试关也拆成岗位专属角色、地点和对白。新增契约函数与测试，后续新增关卡若重复背景会在门禁中失败。

- 验证：25 个剧情场景集合均通过同章背景唯一测试；Node `v24.13.1` 下完整门禁通过，9 个测试文件 / 159 个测试，生产构建和 TeachingBridge 懒加载检查通过。真实浏览器确认前端第 5 章“回归试炼场”入口与学习目标，390×844 下 `scrollWidth = clientWidth = 390`。
- 当前状态：分支仍未提交、未推送、未合并；真人仍需用自己的话完成答案，系统不会自动判定“已经学会”。

## R258：空白用户第一章证据接力验收

使用临时 API `4318`、临时 SQLite 和 Vite `5174`，从空白用户真实走过序章「调取现场证据」→领取委托→第 1 章→开始闯关→前端舞台两条线索→主动复述→传送门大厅。浏览器中明确看到“前端页面 → POST /api/canvases → 后端接口”，并解释 `response.ok` 是收到回信后的判断、201 只是回执而不是数据库入库凭证；复述封存后，下一幕显示上一幕判断回声、传送门守卫和“201 印章不是档案收据”。

- 验证：真实 `390×844` 下 `scrollWidth = clientWidth = 390`；临时数据空间未接触正式学习记录，隔离 API/SQLite 已停止。
- 当前状态：分支仍未提交、未推送、未合并；第一章前两地点的理解链已实测，后续地点和沙盒修复仍需继续人工验收。

## R259：第一章从剧情到实战入口验收

在同一隔离空白用户空间继续走完第一章四个地点：前端舞台、传送门大厅、档案库深处、修复台。每幕都完成两条线索、判断选择和主动复述，第三幕明确呈现“临时内存导致刷新丢失”，第四幕明确呈现 INSERT、刷新和 SELECT 的验收路线。结案后进入实战会合，再进入“读取项目材料”阶段；实战页首屏显示当前任务、上一棒/下一棒交接、Network/日志/数据库/代码材料和关键行阅读模式，并明确不执行终端命令、不读取其他项目。

- 验证：真实隔离浏览器从序章走到第一章实战材料入口；正式记录未污染。第一章教学链和实战入口的 390×844 无横向溢出已在前一轮实测，沙盒修复与报告回读仍必须由用户手动运行测试后完成。
- 当前状态：分支仍未提交、未推送、未合并；不能把进入实战材料页冒充成修复完成或能力掌握。

## R260：测试报告闭环边界验收

在隔离副本中真实运行 `sandbox/canvas-save-persistence` 的 `npm test`，确认故障版本会生成 `1 passed / 1 failed` 的固定 `test-results.json`，且应用只读取报告，不替用户执行命令。补充服务端报告读取器的“尚未生成报告”和“损坏 JSON”自动化测试，锁定等待、失败、源码不匹配、无效报告和通过状态的边界。

- 验证：隔离副本真实测试生成报告并按预期失败；报告边界测试已纳入完整门禁。
- 当前状态：分支仍未提交、未推送、未合并；还剩用户在自己的沙盒中完成修复、手动运行测试并点击“读取结果”，然后再由浏览器复核通过后的结案链路。

## R261：全路线实战九件套契约

补充全路线内容契约测试，覆盖 AI 15 章、Java 5 章和前端 5 章。每个实战配置现在都必须具备独立场景、至少四段流程、基线说明、固定沙盒、至少三项必填作答、验收步骤、四份以上材料导览、通过后的能力记录与至少三条面试/迁移提示；测试还会确认传入的场景 ID 不会静默回退到第一章默认配置。

- 验证：定向 App 测试 43 条通过；Node `v24.13.1` 下完整 `npm run verify` 通过，9 个测试文件 / 162 个测试、生产构建和 TeachingBridge 懒加载检查通过。
- 当前状态：分支仍未提交、未推送、未合并；全路线浏览器逐章视觉抽检和真人迁移效果仍未声称完成。

## R265：TeachingBridge 按需加载过渡页

TeachingBridge 继续保持 `React.lazy` 按需加载，并把懒加载期间的通用“正在展开本章卷宗”替换为统一剧情过渡页：显示当前委托、关卡标题、抵达地点、本关流程和“剧情现场 → 关键代码 → 证据验收”路线，同时使用本关场景背景，避免进入实战时出现白屏或信息断层。

- 验证：Node `v24.13.1` 下 `npm run format:check`、`npm run verify:quick` 通过，9 个测试文件 / 164 个测试通过；`npm run build` 通过，产物检查确认 `TeachingBridge` 仍为独立懒加载 chunk；正式 `127.0.0.1:5173` 浏览器入口可打开且无白屏。
- 当前状态：分支仍未提交、未推送、未合并；还需继续逐章视觉抽检，以及用户手动修复沙盒、运行测试、读取通过报告和完成跨项目迁移复测。

## R266：全量门禁与首关浏览器验收

本轮用项目要求的 Node `v24.13.1` 重跑完整门禁，确认前一轮新增的剧情化加载过渡没有破坏主应用。隔离浏览器从空白用户走通序章、领取委托、第 1 章主线和前端舞台，首屏能看到角色、当前委托、完整交接链和两条探索线索；桌面首屏无横向溢出，移动尺寸 `390×844` 的首关链路已在前一轮实测无横向溢出。

- 验证：`npm run verify` 通过，9 个测试文件 / 164 个测试通过；生产构建通过，`TeachingBridge` 保持独立懒加载 chunk；隔离 API、SQLite 和 Vite 服务已停止。
- 环境备注：系统默认 Node `v18.20.8` 不支持本项目服务端所需的 `node:sqlite`，验证必须使用 `/Users/miduoduo/.nvm/versions/node/v24.13.1/bin`。
- 环境固定：根目录新增 `.nvmrc`，后续进入项目后执行 `nvm use` 即可切换到 `v24.13.1`；`package.json` 已声明 Node `>=22.5.0`。
- 当前状态：分支仍未提交、未推送、未合并；仍需后续逐章视觉抽检，以及用户手动修复沙盒、运行测试、读取通过报告和完成跨项目迁移复测。

## R267：章节角色变化内容门

上一轮内容审计发现第 8 章和第 10–15 章的多个地点虽然有不同背景，但仍重复使用同一个说话角色，容易让用户感觉只是换皮。现在按地点补齐角色身份和对应立绘：资料袋管理员、拒答守门人、参数抄写员、权限守卫、审计取证官、单元符文师、接口接力官、Diff 取证师、恢复审查官、追问审查官等。

- `getTeachingStoryScenes` 暴露只读内容契约供测试使用；新增测试逐一检查 25 条路线关卡的地点、背景、角色、对白、导师解释和至少两条线索。
- 验证：定向 `chapterCinematics.test.ts` 通过，6 个测试通过；随后用 Node `v24.13.1` 完整 `npm run verify` 通过，9 个测试文件 / 165 个测试、生产构建和 `TeachingBridge` 懒加载 chunk 检查全部通过。
- 当前状态：分支仍未提交、未推送、未合并；仍需全路线浏览器视觉抽检、真人沙盒修复、报告回读和跨项目迁移复测。

## R268：路线抽检串线修复

隔离 API `4326` 与 Vite `5178` 中，从空白用户走到序章、证据优先选择和路线大厅，抽检 AI、Java 后端、前端工程三条入口。发现两处会破坏沉浸和理解的问题：第 9 章“知识馆守卷人”仍使用旧 SVG 立绘；Java/前端路线的“后续主线关卡”折叠区固定读取 AI 15 章，导致 5 章路线也显示“第 2 至 15 章”。

- 已把 `App`、`TeachingBridge` 和 `transferRetests` 的知识馆守卷人运行时引用切到统一 WebP 立绘，浏览器复核卷宗中不再出现旧 SVG 图片。
- 已把后续主线折叠区改为读取 `selectedRoute.chapters.slice(1)`，Java/前端路线现在显示“第 2 至 5 章”并列出本路线自己的章节，不再串到 AI 第 15 章。
- 已把当前委托说明里的“不要一次吃下全部 15 章”改为读取当前路线章节数；AI 仍显示 15 章，Java/前端显示 5 章。
- 桌面隔离浏览器已复核：序章、领取委托、AI/Java/前端路线切换均无横向溢出；Java 路线折叠区不再包含 AI 第 15 章。
- 验证：Node `v24.13.1` 下完整 `npm run verify` 通过，9 个测试文件 / 166 个测试、生产构建和 `TeachingBridge` 懒加载 chunk 检查全部通过。
- 移动端补验：Playwright CLI 使用 Node `v24.13.1` 和 `390×844` 视口，在隔离 `5179` 从空白用户走到序章、证据优先和路线大厅；AI 路线 `scrollWidth = clientWidth = 390`，无旧 SVG 图片；Java/前端路线均显示“第 2 至 5 章”、5 个本路线节点、`hasAi15 = false`、横向溢出为 `0`。
- 回归测试：新增 `App.test.tsx` 路线大厅测试，锁定 Java/前端当前委托、后续地图和世界地图不再串回 AI 十五章；后续又扩展到 Java 第 2–5 章、前端第 2–5 章的选中卷宗，确保用户点任一岗位后续节点时也不会回到 AI 产品密室或面试议会。定向测试通过，`npm run verify:quick` 通过 9 个测试文件 / 166 个测试。

## R269-R273：资源门禁、复访/移动抽检、AI 全章与岗位深链回归

- R269：新增运行时资源契约测试，防止旧风格 SVG 角色/宠物和旧 PNG 场景图重新被导入；Node `v24.13.1` 下完整 `npm run verify` 通过 9 个测试文件 / 167 个测试。
- R270：正式 `5173` 复访抽检当前通关用户，确认终章归城入口、Java/前端路线均为 5 章，后续显示「第 2 至 5 章」，无白色断层、无 AI 第 15 章串线、console error 为 0。
- R271：独立 Playwright `390×844` 新用户路径抽检，确认序章、证据选择、路线大厅、AI 15 章入口、Java/前端 5 章入口均无横向溢出和串线。
- R272：隔离 API `4331`、SQLite `/tmp/code-quest-r272.sqlite`、Vite `5181` 批量验证 AI 第 1–15 章桌面与移动教学入口；每章可深链进入、点击「开始闯关」、看到剧情角色、本章地点航线、任务契约、流程/术语线索和 WebP 资源。Java 第 1 章、前端第 1 章也完成桌面与移动入口验收。
- R273：新增岗位章节深链 `#chapter-java-2` 至 `#chapter-java-5`、`#chapter-frontend-2` 至 `#chapter-frontend-5`，保留 AI 原有 `#chapter-2` 格式。刷新或直接打开岗位深链会设置正确路线、章节、场景和 TeachingBridge；卷宗按钮也按选中岗位章节进入专属教学，不再被 AI 主线进度吞回「产品密室」。
- R273 同时修掉两个沉浸断层：岗位 TeachingBridge 封面不再显示「AI 开发主线 · Java/前端」错误前缀；通用序章委托不再把所有章节都说成「沿着一次保存请求」，而是按当前地点标题说明本章证据链。
- R273 验证：Node `v24.13.1` 下 `npm run verify:quick` 通过，9 个测试文件 / 169 个测试；定向 `src/App.test.tsx` 46 个测试通过。
- R273 浏览器验收：隔离 API `4333`、临时 SQLite `/tmp/code-quest-r273.sqlite`、Vite `5183`；Playwright 批量打开 Java 第 2–5 章和前端第 2–5 章，在桌面 `1200×760` 与移动 `390×844` 各跑 8 条，共 16 条深链全部通过。每条均可进入教学序章、点击「开始闯关」、看到剧情角色和本章地点航线，暗色背景正常，场景使用 WebP，`scrollWidth === clientWidth`，控制台 error 为 0，且没有「AI 开发主线」「产品密室」「沿着一次保存请求」串线。
- 当前状态：分支仍未提交、未推送、未合并；还需真实沙盒修复与报告回读、跨项目迁移复测、正式完整 `npm run verify` 和合并审查。

## R274：实战通过报告证据护照

实战验收通过态新增「报告证据护照」：在通过测试清单之前显示生成时间、源码指纹前 10 位、通过/失败汇总，以及“仍要说明哪些路径没覆盖、哪些风险要继续人工复测”。这让用户知道绿色通过不是单纯状态灯，而是一份带时间和源码指纹的当前代码证据；同时继续提醒自动化不能替代人工边界说明。

- 验证：`src/App.test.tsx` 通过态测试覆盖报告证据护照；Node `v24.13.1` 下完整 `npm run verify` 通过，9 个测试文件 / 169 个测试、生产构建和 `TeachingBridge` 懒加载 chunk 检查全部通过。
- 构建备注：Vite 仍提示入口 chunk 超过 500kB；构建成功，`TeachingBridge` 仍独立按需加载。后续若做体积治理，应优先继续拆首页同步依赖和资产使用，不能为消除 warning 牺牲剧情/教学可读性。
- 当前状态：分支仍未提交、未推送、未合并；还需真实沙盒修复与报告回读、跨项目迁移复测和合并审查。

## R275：迁移复测报告证据护照

延迟迁移复测页也补齐同一套报告解释层：通过态不再只显示“测试证据通过”，而是在测试清单前展示「复测报告证据护照」，包含生成时间、源码指纹前 10 位、通过/失败汇总，以及“变式差异与人工复测风险”。这用于回答用户在迁移题里的关键困惑：报告为什么属于当前代码、它证明了哪些路径、为什么仍不能只凭绿色合并。

- 验证：新增 `src/TransferRetestLab.test.tsx`，模拟第 11 章 `verification-trial-arena` 迁移复测返回带 `generatedAt`、`sourceHash`、`summary` 和测试清单的通过报告；断言复测页显示「复测报告证据护照」、源码短码、`2 通过 / 0 失败` 和测试名称。
- 已通过：`npm run format:check`；`npm run test -- src/TransferRetestLab.test.tsx --run`；`npm run verify:quick` 通过，10 个测试文件 / 170 个测试；完整 `npm run verify` 通过，生产构建和 `TeachingBridge` 懒加载检查通过。
- 浏览器验收：隔离 API `4335`、临时 SQLite `/tmp/code-quest-r275.sqlite`、Vite `5185`；造第 11 章源实战 24 小时前已提交记录，打开迁移复测折叠区，确认第 11 章卡片显示「延迟复测 · 已解锁」。Playwright 拦截启动请求返回通过报告后，桌面复测页显示「复测报告证据护照」、`9f3a7d12b8`、`2 通过 / 0 失败` 和两条通过测试；390×844 下 `scrollWidth = clientWidth = 390`，证据护照可见，控制台 error 为 0。截图位于 `output/playwright/.playwright-cli/page-2026-07-15T07-40-04-714Z.png`。
- 当前状态：分支仍未提交、未推送、未合并；还需真实沙盒修复与报告回读和合并审查。

## R276：实战材料接力解释条

实战材料阅读页新增「材料接力」解释：每份 Network、日志、数据库或代码材料都会先说明“从哪里来、交给谁、回答什么问题”。这是继续解决用户“我不知道谁传给谁、为什么看这份材料”的理解断点；它和原有导师卡、关键行模式、能证明/不能证明说明一起构成新手阅读脚手架。

- 实现：`ArtifactViewer` 根据材料语言生成接力说明；JSON/Network 指向“浏览器 Network 或测试报告 → 后端接口与验收判断”，日志指向“后端运行现场 → 定位下一处断点”，SQL 指向“数据层或数据库 → 刷新后的事实校验”，代码指向“项目代码 → Agent 修复任务或人工改动”。
- 验证：`src/App.test.tsx` 锁定材料接力解释、Network 来源、交付对象和“这次请求或测试到底返回了什么结果”的问题提示；`npm run format:check` 通过，`npm run test -- src/App.test.tsx --run` 通过 46 个测试；`npm run verify:quick` 通过，10 个测试文件 / 170 个测试；完整 `npm run verify` 通过，生产构建和 `TeachingBridge` 懒加载检查通过。
- 浏览器验收：隔离 API `4336`、临时 SQLite `/tmp/code-quest-r276.sqlite`、Vite `5186`；补齐第一章教学进度后进入实战「读取项目材料」阶段，确认桌面页面显示「材料接力解释」「项目代码 → Agent 修复任务或人工改动」和本材料要回答的问题。390×844 下 `scrollWidth = clientWidth = 390`，材料接力可见，控制台 error 为 0。截图位于 `output/playwright/.playwright-cli/page-2026-07-15T07-59-49-259Z.png`。
- 当前状态：分支仍未提交、未推送、未合并；还需真实沙盒修复与报告回读和合并审查。

## R277：实战当前能力印记

实战页在冒险日志后新增「当前能力印记」：每一步都会显示本关最终要证明的工程能力、当前正在练的动作，以及会沉淀成哪类成长档案/面试素材。这个改动用于增强角色成长反馈，避免用户在实战页只看到表单、材料和报告，而忘记自己正在升级哪种工作能力。

- 实现：新增 `LabAbilityMark`，读取 `config.result.proved`、当前步骤和 `config.result.recorded`；验证步骤会显示“用测试报告证明修复成立”，基线步骤显示“把剧情委托翻译成工作问题”，普通作答步骤显示当前作答标题。
- 验证：`src/App.test.tsx` 锁定「当前能力印记」「正在练」「会沉淀成」和第一章的“排查路径、提示次数与解释”；`npm run format:check` 通过，`npm run test -- src/App.test.tsx --run` 通过 46 个测试；`npm run verify:quick` 通过，10 个测试文件 / 170 个测试；完整 `npm run verify` 通过，生产构建和 `TeachingBridge` 懒加载检查通过。
- 浏览器验收：隔离 API `4337`、临时 SQLite `/tmp/code-quest-r277.sqlite`、Vite `5187`；补齐第一章教学进度后进入实战「读取项目材料」阶段，桌面页面显示「当前能力印记」「代码产生真实数据库副作用」「阶段 02 · 项目材料」「排查路径、提示次数与解释」。390×844 下 `scrollWidth = clientWidth = 390`，能力印记可见，控制台 error 为 0。截图位于 `output/playwright/.playwright-cli/page-2026-07-15T08-12-14-854Z.png`。
- 当前状态：分支仍未提交、未推送、未合并；还需真实沙盒修复与报告回读和合并审查。

## R278：实战步骤收录回执

实战页保存作答后新增「证据已收录」回执：自动跳到下一步时，会显示刚刚收录的步骤名称，并解释这一步已经写入本地练习记录、现在继续把上一棒证据交给下一棒。这个改动专门处理用户反馈的“我忘了上一地点，突然到下一步就晕”的问题。

- 实现：`Lab` 记录最近成功保存的步骤；自动推进时渲染 `LabStepReceipt`，手动点击侧边栏或基线入口时清空回执，避免旧提示停留在错误阶段。
- 验证：`src/App.test.tsx` 已加入主流程断言，填写「读取项目材料」答案并点击「保存并继续」后，应看到「刚刚收录的证据」「证据已收录」「读取项目材料」和“继续把上一棒证据交给下一棒”；`npm run format:check` 通过，`npm run test -- src/App.test.tsx --run` 通过 46 个测试；`npm run verify:quick` 通过，10 个测试文件 / 170 个测试；完整 `npm run verify` 通过，生产构建和 `TeachingBridge` 懒加载检查通过。
- 浏览器验收：隔离 API `4338`、临时 SQLite `/tmp/code-quest-r278.sqlite`、Vite `5188`；从新用户序章进入路线大厅，补齐第 1 章教学进度后进入实战「读取项目材料」，填写三段证据答案并点击「保存并继续」。桌面自动进入「还原数据流」，显示「刚刚收录的证据」「证据已收录」「读取项目材料」和“继续把上一棒证据交给下一棒”。390×844 下 `scrollWidth = clientWidth = 390`，回执仍可见，控制台 error 为 0。截图位于 `.playwright-cli/page-2026-07-15T08-38-23-002Z.png`。
- 当前状态：分支仍未提交、未推送、未合并；还需真实沙盒修复与报告回读和合并审查。

## R279：实战剧情向导

实战练习页在冒险日志后新增「实战剧情向导」：复用当前章节角色立绘，显示角色名、当前实战场景、这一幕先看懂什么、当前流程地点和行动指令。这个改动继续解决用户反馈的“进入实战后像普通表单、人物和剧情断掉”的问题。

- 实现：新增 `LabSceneGuide`，按 `scenarioId` 选择对应章节角色图；基线、作答和测试步骤使用不同的场景名和行动文案。手机端改为头像 + 台词 + 单列行动指令，避免挤压。
- 验证：`src/App.test.tsx` 已加入主流程断言，进入实战「读取项目材料」时应看到「实战剧情向导」、档案馆记录员立绘、「这一幕先看懂：读取项目材料」「当前地点」和「行动指令」；`npm run format:check` 通过，`npm run test -- src/App.test.tsx --run` 通过 46 个测试；`npm run verify:quick` 通过，10 个测试文件 / 170 个测试；完整 `npm run verify` 通过，生产构建和 `TeachingBridge` 懒加载检查通过。
- 浏览器验收：隔离 API `4339`、临时 SQLite `/tmp/code-quest-r279.sqlite`、Vite `5189`；从新用户序章进入路线大厅，补齐第 1 章教学进度后进入实战「读取项目材料」。桌面可见「实战剧情向导」、档案馆记录员立绘、「这一幕先看懂：读取项目材料」、当前地点「发出 POST」和行动指令。390×844 下 `scrollWidth = clientWidth = 390`，向导正文完整，控制台 error 为 0。截图位于 `.playwright-cli/page-2026-07-15T09-00-20-050Z.png`。
- 当前状态：分支仍未提交、未推送、未合并；还需真实沙盒修复与报告回读和合并审查。

## R280：实战本步任务卷轴

实战练习页新增「本步任务卷轴」：每一步都压缩成“为什么学 / 先看什么 / 最后交什么”三段短说明。这个改动继续处理用户反馈的“看着晕，不知道这一步到底要干嘛”，让剧情、材料、输入框之前先有一个学习目标锚点。

- 实现：新增 `LabStepQuestBrief`，根据当前步骤类型和章节配置生成学习目的、证据入口和最终产出；手机端改为单列，避免横向挤压。
- 验证：`src/App.test.tsx` 已加入主流程断言，进入实战「读取项目材料」时应看到「本步任务卷轴」「为什么学」「先看什么」「最后交什么」和当前步骤目标；`npm run format:check` 通过，`npm run test -- src/App.test.tsx --run` 通过 46 个测试；`npm run verify:quick` 通过，10 个测试文件 / 170 个测试。
- 浏览器验收：隔离 API `4340`、临时 SQLite `/tmp/code-quest-r280.sqlite`、Vite `5190`；从新用户序章进入路线大厅，补齐第 1 章教学进度后进入实战「读取项目材料」。桌面可见「本步任务卷轴」「为什么学」「先看什么」「最后交什么」和当前步骤目标。390×844 下 `scrollWidth = clientWidth = 390`，卷轴正文完整，控制台 error 为 0。截图位于 `.playwright-cli/page-2026-07-15T09-25-28-102Z.png`。
- 完整门禁：Node `v24.13.1` 下 `npm run verify` 通过，包含格式、Lint、类型、10 个测试文件 / 170 个测试、生产构建和 TeachingBridge 懒加载检查。Vite 主包体积 warning 仍是已知债务，不是失败。
- 当前状态：代码、测试断言、changelog、浏览器验收证据和完整门禁证据已补；分支仍未提交、未推送、未合并。

## R281：实战流程接力小剧场

实战练习页新增「流程接力小剧场」：在任务卷轴后插入上一棒、当前棒、下一棒三段短对白，把当前材料放进“谁把什么交给谁”的工程链路。这个改动继续处理用户反馈的“response.ok 到底传给谁、下一地点为什么出现”的困惑。

- 实现：新增 `LabFlowDialogue`，根据当前步骤和 `config.flowItems` 生成上一站、当前站、下一站对白；桌面三列接力，390px 手机端单列纵向接力。
- 验证：`src/App.test.tsx` 已加入主流程断言，进入实战「读取项目材料」时应看到「流程接力小剧场」「上一棒」「当前棒」「下一棒」以及前端、后端接口接力；`npm run format:check` 通过，`npm run test -- src/App.test.tsx --run` 通过 46 个测试；`npm run verify:quick` 通过，10 个测试文件 / 170 个测试。
- 浏览器验收：隔离 API `4341`、临时 SQLite `/tmp/code-quest-r281.sqlite`、Vite `5191`；进入第 1 章实战「读取项目材料」后，桌面可见「流程接力小剧场」、上一棒「用户」、当前棒「前端」、下一棒「后端接口」和三段对白。390×844 下 `scrollWidth = clientWidth = 390`，小剧场正文完整，控制台 error 为 0。截图位于 `.playwright-cli/page-2026-07-15T09-40-03-708Z.png`。
- 完整门禁：Node `v24.13.1` 下 `npm run verify` 通过，包含格式、Lint、类型、10 个测试文件 / 170 个测试、生产构建和 TeachingBridge 懒加载检查。Vite 主包体积 warning 仍是已知债务，不是失败。
- 当前状态：代码、测试断言、changelog、浏览器验收证据和完整门禁证据已补；分支仍未提交、未推送、未合并。

## R282：材料代码三步翻译卡

实战材料阅读区新增「代码三步翻译卡」：在关键行聚焦后，把关键代码片段翻译成它在工程流程里“接到什么、判断什么、交给下一棒什么”。这个改动继续降低用户读 `fetch / response.ok / setStatus` 时的理解成本。

- 实现：新增 `explainKeyLine`，对 `fetch/POST`、`response.ok/201`、`setStatus/onSaved/saved`、`SELECT/0 rows` 和测试报告生成不同的人话解释；`ArtifactViewer` 只取前三条关键线索，避免信息过载。
- 验证：`src/App.test.tsx` 已加入主流程断言，进入实战「读取项目材料」时应看到「代码三步翻译卡」「把动作送出去」「判断接口有没有接住」和“不能直接证明数据库已经写入”；`npm run format:check` 通过，`npm run test -- src/App.test.tsx --run` 通过 46 个测试；`npm run verify:quick` 通过，10 个测试文件 / 170 个测试。
- 浏览器验收：隔离 API `4342`、临时 SQLite `/tmp/code-quest-r282.sqlite`、Vite `5192`；进入第 1 章实战「读取项目材料」后，桌面可见「代码三步翻译卡」「把动作送出去」「判断接口有没有接住」和“不能直接证明数据库已经写入”。390×844 下 `scrollWidth = clientWidth = 390`，翻译卡正文完整，控制台 error 为 0。截图位于 `.playwright-cli/page-2026-07-15T10-00-30-035Z.png`。
- 完整门禁：Node `v24.13.1` 下 `npm run verify` 通过，包含格式、Lint、类型、10 个测试文件 / 170 个测试、生产构建和 TeachingBridge 懒加载检查。Vite 主包体积 warning 仍是已知债务，不是失败。
- 当前状态：代码、测试断言、changelog、浏览器验收证据和完整门禁证据已补；分支仍未提交、未推送、未合并。

## R283：作答证据表达示范卡

实战作答区新增「证据表达示范卡」：放在作答支架和输入框之间，用“我看到 / 它说明 / 下一步”示范怎么把证据写成可保存的判断。这个改动继续解决用户能看懂材料但不知道怎么表达的问题。

- 实现：`ResponseForm` 新增普通证据题和 Agent 委托题两套示范短句；普通证据题强调 `POST` 和 `response.ok` 只能证明页面收到成功信号，不能证明数据库写入。
- 验证：`src/App.test.tsx` 已加入主流程断言，进入实战「读取项目材料」时应看到「证据表达示范卡」「前端发出了 POST」「还不能证明数据库真的写入」和“继续查后端日志和 SELECT 结果”；`npm run format:check` 通过，`npm run test -- src/App.test.tsx --run` 通过 46 个测试；`npm run verify:quick` 通过，10 个测试文件 / 170 个测试。
- 浏览器验收：隔离 API `4343`、临时 SQLite `/tmp/code-quest-r283.sqlite`、Vite `5193`；进入第 1 章实战「读取项目材料」后，桌面可见「证据表达示范卡」「前端发出了 POST」「还不能证明数据库真的写入」和“继续查后端日志和 SELECT 结果”。390×844 下 `scrollWidth = clientWidth = 390`，示范卡正文完整，控制台 error 为 0。截图位于 `.playwright-cli/page-2026-07-15T10-28-18-491Z.png`。
- 完整门禁：Node `v24.13.1` 下 `npm run verify` 通过，包含格式、Lint、类型、10 个测试文件 / 170 个测试、生产构建和 TeachingBridge 懒加载检查。Vite 主包体积 warning 仍是已知债务，不是失败。
- 当前状态：代码、测试断言、changelog、浏览器验收证据和完整门禁证据已补；分支仍未提交、未推送、未合并。

## R284：实战保存能力沉淀回执

实战保存回执新增「刚刚沉淀 / 下一步验证 / 以后可复盘」三项，让用户保存一段作答后知道它会变成什么学习证据、下一题为什么继续，而不是只看到“保存成功并跳下一步”。

- 实现：扩展 `LabStepReceipt`，根据已保存步骤和下一步骤生成能力沉淀、下一步验证方向和本关复盘产出；手机端三项单列显示。
- 验证：`src/App.test.tsx` 已加入主流程断言，保存「读取项目材料」后应看到「刚刚沉淀」「下一步验证」「以后可复盘」和“一段可复查的读取项目材料判断”。
- 已验证：Node `v24.13.1` 下 `npm run format:check`、`npm run test -- src/App.test.tsx --run`（46 个测试）和 `npm run verify:quick`（10 个测试文件 / 170 个测试）通过。
- 浏览器验收：隔离 API `4344`、临时 SQLite `/tmp/code-quest-r284.sqlite`、Vite `5194`，从第 1 章进入实战「读取项目材料」，保存三段证据答案后进入「还原数据流」。桌面和 390×844 下均显示「刚刚收录的证据」「刚刚沉淀」「下一步验证」「以后可复盘」和“一段可复查的读取项目材料判断”；手机端 `scrollWidth = clientWidth = 390`，控制台 error 为 0，截图在 `.playwright-cli/page-2026-07-15T10-58-46-901Z.png`。
- 完整门禁：Node `v24.13.1` 下 `npm run verify` 通过，包含格式、Lint、类型、10 个测试文件 / 170 个测试、生产构建和 TeachingBridge 懒加载检查。Vite 主包体积 warning 仍是已知债务，不是失败。
- 当前状态：代码、测试断言、changelog、任务文档、浏览器验收证据和完整门禁证据已补；分支仍未提交、未推送、未合并。

## R285：第一章实战步骤场景导演层

第一章实战页新增逐步场景导演层：每个 `LabStep` 可以配置自己的地点、背景、角色、台词、目标和通关收获。第一章 8 个步骤现在分别绑定档案馆记录员、传送门书记官、接口接待员、回声取证官、任务锻造师、交付审判官、镜面编辑师和面试策士，避免用户在实战里一直看到同一个角色和同一张背景。

- 实现：`LabSceneGuide` 和实战外层背景读取当前步骤 `scene`；保存「读取项目材料」后，页面从「传送门书记官 · 前端讯号窗」切到「接口接待员 · 请求中转门」，同时保留「刚刚收录的证据」回执。
- 验证：`src/App.test.tsx` 主流程覆盖第一步场景和保存后的第二步场景；Node `v24.13.1` 下 `npm run format:check`、`npm run test -- src/App.test.tsx --run`、`npm run verify:quick` 和完整 `npm run verify` 均通过。
- 浏览器验收：隔离 API `4345`、临时 SQLite `/tmp/code-quest-r285.sqlite`、Vite `5195`；从第 1 章教学完成页进入实战，桌面确认第一步显示「传送门书记官」「前端讯号窗」，填写证据答案保存后自动进入「还原数据流」，显示「接口接待员」「请求中转门」和「刚刚收录的证据」。
- 移动验收：390×844 下 `scrollWidth = clientWidth = 390`，向导与回执完整可读，控制台 error 为 0；截图在 `.playwright-cli/page-2026-07-15T18-51-10-368Z.png`。
- 当前状态：代码、测试断言、changelog、任务文档、浏览器验收证据和完整门禁证据已补；分支仍未提交、未推送、未合并。

## R286：第 2 章产品链路实战场景导演层

第 2 章 CanvasStorm 实战页也补上逐步场景导演层：读 Brief、筛方向候选、会话保存、Agent 委托、交付审查和面试复盘都会切换不同地点、角色、背景、目标和通关收获。这样第 2 章不会在进入实战后掉回通用表单感，也不会复用第 1 章的前端/数据库剧情语义。

- 实现：`canvasstorm-product-brief` 的 `LabStep.scene` 覆盖 7 个步骤；首个作答步骤为「灵感萤火 · Brief 星图桌」，保存后进入「产品链路带读官 · 方向筛选台」。
- 验证：`src/App.test.tsx` 主流程覆盖第 2 章进入实战后的首幕场景和保存后的第二幕场景；Node `v24.13.1` 下 `npm run test -- src/App.test.tsx --run` 通过 46 个测试。
- 当前状态：代码、测试断言、changelog 和任务文档已补；`npm run test -- src/App.test.tsx --run` 与 `npm run verify:quick` 已通过。仍需按时间决定是否补浏览器抽检；提交推送前不得写成已合并或学习效果已验证。

## R287：第 2 章产品链路新手翻译

第 2 章实战在场景导演层之上补充专属学习解释：每个步骤可以配置自己的「为什么学 / 先看什么 / 最后交什么」和流程接力对白，避免产品链路继续套用第一章保存链路的通用话术。

- 实现：`LabStep` 新增 `questBrief` 与 `flowDialogue`；第 2 章 7 个步骤已补齐产品链路专属解释。读 Brief 会说明 Project Brief 是 AI 功能的任务契约；筛候选会用“候选池像装备栏”解释 MVP 取舍。
- 验证：`src/App.test.tsx` 已加入第 2 章首幕和保存后第二幕断言；Node `v24.13.1` 下 `npm run test -- src/App.test.tsx --run` 通过 46 个测试，`npm run verify:quick` 通过 10 个测试文件 / 170 个测试。
- 当前状态：代码、测试断言、changelog 和任务文档已补；仍未声称浏览器抽检或真人学习效果完成。

## R288：第 2 章浏览器验收与串章修复

第 2 章教学完成页曾在真实浏览器里显示「AI 状态台 / 安全记录员」，这是第 7 章 AI API 安全内容，不应出现在产品链路章。本轮把第 2 章第四个教学地点改为「产品复盘厅 / 面试策士」，收束 Brief、方向取舍、会话保存和面试表达。

- 实现：`canvasStormScenes` 和 `canvasStormJourney` 移除 `cs-ai-status`，新增 `cs-product-recap`；第 2 章仍保持 4 个教学地点，但不再提前讲 API Key。
- 测试：`src/chapterCinematics.test.ts` 增加断言，确保第 2 章包含产品复盘厅、不包含 AI 状态台、不包含 API Key。
- 浏览器验收：隔离 API `4348`、临时 SQLite `/tmp/code-quest-r288.sqlite`、Vite `5198`。桌面第 2 章会合页显示「产品复盘厅 / 面试策士」；实战首幕显示「灵感萤火」「Brief 星图桌」「Project Brief 是 AI 功能的任务契约」和关键行导读；保存 Brief 后进入「产品链路带读官 · 方向筛选台」，桌面 `clientWidth = scrollWidth = 1200`。
- 移动抽检：390×844 下第二幕显示「产品链路带读官」「方向筛选台」「真实 AI 产品不是把所有点子都做进去」「候选池像装备栏」，`clientWidth = scrollWidth = 390`，背景为暗色，console error 为 0。截图：`.playwright-cli/page-2026-07-16T08-33-50-456Z.png`。
- 自动化验证：Node `v24.13.1` 下 `npm run test -- src/chapterCinematics.test.ts src/App.test.tsx --run` 通过 2 个测试文件 / 53 个测试；`npm run verify:quick` 通过 lint、typecheck、10 个测试文件 / 170 个测试。
- 当前状态：浏览器证据、代码修复、自动化验证和交接记录已补；等待本轮提交和推送。

## R289：教学桥控件暗色 RPG 收口

本轮继续处理用户指出的“风格断层/像白色课件后台”的问题，范围收敛在教学桥关键控件，不重写业务数据。

- 实现：`src/styles.css` 补齐概念卡、预测题、预测错误态、解释框、流程图节点编号、节点接力标签和 footer 继续提示的暗色 RPG 覆盖。
- 测试：`src/chapterCinematics.test.ts` 增加样式回归断言，要求这些关键教学控件有具体暗色规则块，且不回退到 `background: white` / `#fff`。
- 文档：`docs/ai-career-rpg-tasks.md` 新增 R289 记录；新增 changelog fragment `changelogs/2026-07-16-teaching-control-dark-rpg.md`。
- 浏览器验收：隔离 API `4350`、临时 SQLite `/tmp/code-quest-r289.sqlite`、Vite `5200`。第 1 章剧情页显示完整流程卷轴、名词小抄、本幕复盘和下一地点预告；桌面 `clientWidth = scrollWidth = 1200`，390×844 下 `clientWidth = scrollWidth = 390`，body 背景为暗色，console error 为 0。
- 自动化验证：Node `v24.13.1` 下 `npm run test -- src/chapterCinematics.test.ts --run` 通过 1 个测试文件 / 8 个测试；`npm run format:check` 通过；`npm run verify:quick` 通过 lint、typecheck、10 个测试文件 / 171 个测试。
- 当前状态：本轮 UI 收口、测试、浏览器证据和交接记录已补；仍未声称“全站最终 UI 完成”，后续应继续按章节检查白色断层和学习可理解性。

## R290：代码导读当前行证据锚点

本轮继续处理用户说的“看代码还是晕，不知道谁传给谁、下一步去哪”的问题，范围收敛在 `GuidedCodeTour`。

- 实现：`src/TeachingBridge.tsx` 新增 `buildLineEvidenceAnchor`，并在逐行翻译卡片中展示“为什么看这一行 / 检查点 / 下一份证据”。第 1 章读到 `response.ok` 时会明确提醒它只解释绿色成功提示，不能证明数据库已经写入。
- UI：`src/styles.css` 新增 `.line-evidence-anchor` 暗色任务卡；390px 下改成单列，避免代码阅读页挤压。
- 测试：`src/App.test.tsx` 覆盖第 1 章和第 2 章代码导读的新锚点；第 1 章会断言 `response.ok` 行出现“绿色成功提示为什么会亮”“不能证明数据库已经写入”“数据库 SELECT 结果”。
- 文档：`docs/ai-career-rpg-tasks.md` 新增 R290 记录；新增 changelog fragment `changelogs/2026-07-16-code-tour-evidence-anchor.md`。
- 自动化验证：Node `v24.13.1` 下 `npm run test -- src/App.test.tsx --run` 通过 1 个测试文件 / 46 个测试；`npm run verify:quick` 通过 lint、typecheck、10 个测试文件 / 171 个测试。
- 浏览器验收：隔离 API `4352`、临时 SQLite `/tmp/code-quest-r290.sqlite`、Vite `5202`。第 1 章 `tour-frontend` 代码导读页显示当前行证据锚点；跳到 `response.ok` 行后可见“绿色成功提示为什么会亮”“不能证明数据库已经写入”“Network 状态码、后端日志、数据库 SELECT 结果”。桌面 `clientWidth = scrollWidth = 1200`，390×844 下 `clientWidth = scrollWidth = 390`，body 背景为暗色，console error 为 0。
- 当前状态：代码导读页更接近“任务指引式读码”，但仍未声称第一章全流程和全站最终体验已经完成；后续应继续验收实战修复页、章节结算页和 2-15 章读码页的一致性。
