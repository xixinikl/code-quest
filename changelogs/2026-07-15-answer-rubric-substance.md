# 作答检查从标签变成有内容

实战作答支架不再因为出现“背景 / 约束 / 验收”几个标签就全部亮起。Agent 委托必须在这些标签后写出实际内容；普通证据题必须同时出现证据、证据含义和下一步验证动作。它只负责拦截明显空答案，不替代真人理解判断。

- 验收：点击「填入骨架」后 Agent 委托仍显示 `0/3`，填入有内容的答案后才完成；普通证据答案保留 `3/3`。
- 验证：Node `v24.13.1` 下完整 `npm run verify` 通过，9 个测试文件 / 157 个测试、生产构建和 TeachingBridge 懒加载检查通过。
