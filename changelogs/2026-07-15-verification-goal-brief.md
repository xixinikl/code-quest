# 实战验收先说清证明目标

实战验收页新增「本次验收目标」：先告诉学习者这次要证明修复能被测试复现并通过，再说明通过后会解锁下一棒并写入成长档案。手动操作仍保持四步顺序，应用只读取固定测试报告，不替用户执行命令；按钮改为「读取结果」，减少“报告从哪里来”的疑惑。

- 验收：真实 `1280×900` 与 `390×844` 浏览器确认目标卡、通过结果和「读取结果」按钮存在，`scrollWidth = clientWidth`。
- 验证：Node `v24.13.1` 下完整 `npm run verify` 通过，9 个测试文件 / 155 个测试、生产构建和 TeachingBridge 懒加载检查通过。
